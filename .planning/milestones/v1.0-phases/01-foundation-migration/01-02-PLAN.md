---
phase: 01-foundation-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/pages/index.astro
  - src/components/Hero.astro
  - src/components/ServiceCard.astro
  - src/components/ServicesGrid.astro
  - src/components/TechGrid.astro
  - src/components/ProjectCard.astro
  - src/components/TestimonialCard.astro
  - src/components/ContactCTA.astro
  - src/styles/global.css
autonomous: false
requirements:
  - FOUND-06
  - FOUND-07

must_haves:
  truths:
    - "Homepage displays hero section with badge, headline, subtitle, and two CTA buttons"
    - "Homepage displays services grid with at least 3 service cards showing icon, title, and description"
    - "Homepage displays tech stack grid with technology icons from Devicon CDN, not just text labels"
    - "Homepage displays portfolio preview section with 2-3 placeholder project cards linking to /portfolio"
    - "Homepage displays testimonials preview section with 2-3 placeholder testimonial cards"
    - "Homepage displays a contact CTA section linking to /contact"
    - "All pages render correctly at 375px, 768px, and 1200px viewport widths without horizontal scroll or overlapping elements"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Homepage hero with badge, title, subtitle, CTAs"
      min_lines: 20
    - path: "src/components/TechGrid.astro"
      provides: "Tech stack grid with Devicon icons"
      contains: "cdn.jsdelivr.net/gh/devicons"
    - path: "src/components/ServicesGrid.astro"
      provides: "Services section with service cards"
      contains: "service-card"
    - path: "src/components/ProjectCard.astro"
      provides: "Portfolio preview card component"
      contains: "project-card"
    - path: "src/components/TestimonialCard.astro"
      provides: "Testimonial preview card component"
      contains: "testimonial"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/Hero.astro"
      via: "component import"
      pattern: "import Hero"
    - from: "src/pages/index.astro"
      to: "src/components/TechGrid.astro"
      via: "component import"
      pattern: "import TechGrid"
    - from: "src/pages/index.astro"
      to: "src/components/ServicesGrid.astro"
      via: "component import"
      pattern: "import ServicesGrid"
    - from: "src/components/TechGrid.astro"
      to: "Devicon CDN"
      via: "img src attribute"
      pattern: "cdn\\.jsdelivr\\.net.*devicon"
---

<objective>
Migrate all homepage sections from the original index.html into Astro components and assemble them on the homepage. Add tech stack icons via Devicon, create portfolio and testimonials preview sections with placeholder data, and ensure full responsive polish across all pages.

Purpose: Delivers the refined, content-rich homepage that demonstrates the agency's services and capabilities, and ensures the entire site works across all device sizes.
Output: Complete homepage with Hero, Services, Tech Stack (with icons), Portfolio preview, Testimonials preview, and Contact CTA. All pages responsive.
</objective>

<execution_context>
@/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-migration/01-CONTEXT.md
@.planning/phases/01-foundation-migration/01-RESEARCH.md
@.planning/phases/01-foundation-migration/01-01-SUMMARY.md

@index.html
@style.css

<interfaces>
<!-- From Plan 01 (expected outputs): -->

BaseLayout.astro provides:
- Props: { title: string, description?: string }
- Wraps content with nav, footer, background glows, View Transitions
- Imports global.css

PageHeader.astro provides:
- Props: { title: string, subtitle?: string }
- Renders section with gradient h1 and optional subtitle

Navigation.astro provides:
- 6 nav links + CTA button
- Mobile sidebar with hamburger toggle

global.css provides:
- All CSS custom properties from :root
- .glass-panel, .text-gradient, .btn variants, .reveal/.active
- .services-grid, .service-card, .projects-grid, .project-card, .tech-grid, .tech-item
- .hero, .hero-content, .hero-title, .hero-subtitle, .hero-cta, .badge
- .contact-wrapper, .contact-form, .glass-input, .form-group
- Responsive breakpoints at 900px and 600px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create homepage section components</name>
  <files>
    src/components/Hero.astro,
    src/components/ServiceCard.astro,
    src/components/ServicesGrid.astro,
    src/components/TechGrid.astro,
    src/components/ProjectCard.astro,
    src/components/TestimonialCard.astro,
    src/components/ContactCTA.astro
  </files>
  <action>
1. Create `src/components/Hero.astro`:
   - Port the hero section from `index.html`: badge ("Future-Proof Engineering"), title ("We Build Digital Products That Scale."), subtitle, and two CTA buttons.
   - Update CTA links: "Partner With Us" -> `/contact`, "Explore Expertise" -> `/portfolio`.
   - Add `class="reveal"` for scroll animation.
   - Claude's discretion on whether to refresh the hero copy slightly for more impact.

2. Create `src/components/ServiceCard.astro`:
   - Props: `{ icon: string, title: string, description: string, delay?: string }`
   - Renders a `.service-card.glass-panel.reveal` with the icon, h3 title, and p description.
   - Apply `style={delay ? `--delay: ${delay}` : ''}` for staggered reveal.

3. Create `src/components/ServicesGrid.astro`:
   - Define services data array inline (at least 3 services matching existing: Web & Mobile Dev, DevOps & Cloud, Analytics & Insights). Claude's discretion on expanding to 4-6 services.
   - Render section with `.section-heading` ("Core Expertise") and `.services-grid` mapping over ServiceCard components.

4. Create `src/components/TechGrid.astro`:
   - Define technologies array: `[{name: 'Laravel', icon: 'laravel'}, {name: 'Flutter', icon: 'flutter'}, {name: 'Kubernetes', icon: 'kubernetes'}, {name: 'Terraform', icon: 'terraform'}, {name: 'Docker', icon: 'docker'}, ...]`
   - For each tech item, render an `<img>` with `src="https://cdn.jsdelivr.net/gh/devicons/devicon@latest/icons/{icon}/{icon}-original.svg"` (or `-plain` variant for better visibility on dark backgrounds).
   - Add `width="40" height="40"` and `alt` attributes.
   - For icons that may not exist in Devicon (FilamentPHP, GTM), use the PHP icon for Filament and Google icon for GTM, or use `-plain` variants with appropriate color filters for dark theme visibility.
   - Wrap in `.tech-stack.section.full-width` with `.section-heading` ("Our Technology Arsenal") and `.tech-grid`.
   - Update `.tech-item` to use flexbox column layout: icon above, name below.

5. Create `src/components/ProjectCard.astro`:
   - Props: `{ tag: string, title: string, description: string, delay?: string }`
   - Renders a `.project-card.glass-panel.reveal` with project tag, h3, description, and "View Details" link pointing to `/portfolio`.
   - These are placeholder cards -- the real data comes from GitHub API in Phase 2.

6. Create `src/components/TestimonialCard.astro`:
   - Props: `{ quote: string, name: string, role: string, company: string, delay?: string }`
   - Renders a glass-panel card with the quote text, and attribution (name, role at company).
   - Add scoped styles for testimonial layout: quote in larger/italic text, attribution below in smaller text.
   - These are placeholder -- real data comes from config in Phase 2.

7. Create `src/components/ContactCTA.astro`:
   - A simpler call-to-action section (not the full form -- that's on /contact page).
   - Heading: "Ready to Accelerate?" with gradient text.
   - Subtitle paragraph.
   - CTA button linking to `/contact`: "Get In Touch".
   - Styled as a centered section with a glass-panel card.
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 7 homepage components exist and build without errors. TechGrid uses Devicon CDN URLs for technology icons. ServiceCard, ProjectCard, and TestimonialCard accept props for reusable rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble homepage and polish responsive design</name>
  <files>
    src/pages/index.astro,
    src/styles/global.css
  </files>
  <action>
1. Update `src/pages/index.astro` to import and render all homepage components in order:
   - Hero (full-viewport hero, NOT using PageHeader)
   - ServicesGrid
   - TechGrid (full-width section)
   - Portfolio preview section: section heading ("Featured Projects") + 2-3 ProjectCard components with placeholder data matching the original index.html projects (Enterprise CRM, Microservices, FinTech Wallet). Add a "View All Projects" link to `/portfolio` below the cards.
   - Testimonials preview section: section heading ("What Clients Say") + 2-3 TestimonialCard components with placeholder testimonial data (fictional but realistic agency testimonials). Add a "Read More Testimonials" link below.
   - ContactCTA

2. Add or update styles in `src/styles/global.css` for:
   - Testimonials grid layout: `display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem`
   - Testimonial card styles: quote marks, italic text, attribution styling
   - "View all" link styling (centered, subtle, with arrow)
   - Tech item with icon: flex-direction column, gap between icon and text, icon sizing
   - Ensure `.full-width` class works correctly (breaks out of container for full-bleed background)

3. Responsive polish across ALL pages (not just homepage):
   - At 768px and below: hamburger menu visible, desktop nav links hidden. Services grid collapses to 1 column. Projects grid to 1 column. Tech grid to 3 columns. Contact form grid to 1 column.
   - At 375px: reduce section padding, font sizes scale down gracefully via clamp(), hero CTA buttons stack vertically.
   - Ensure inner pages (portfolio, team, blog, pricing, contact) have correct page header spacing and no layout issues at any viewport.
   - Test that `.page-header` has enough top padding to clear the fixed nav (~10rem).
   - Verify no horizontal scrollbar appears at any viewport width.

4. Add any missing CSS for the footer expansion (if Plan 01 created an expanded footer -- add responsive styles for footer columns collapsing on mobile).
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build && grep -c "devicon" dist/index.html && grep -c "testimonial" dist/index.html</automated>
  </verify>
  <done>Homepage renders all sections: Hero, Services, Tech Stack (with icons), Portfolio preview (2-3 cards), Testimonials preview (2-3 cards), Contact CTA. All pages responsive at 375px, 768px, and 1200px without horizontal scroll or layout breakage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete site</name>
  <files>none</files>
  <action>
User visually verifies the complete Astro site across all pages and viewport sizes.

What was built: Complete Astro site with 6 pages, glassmorphism design system, responsive navigation with mobile sidebar, and full homepage with all sections including tech icons, portfolio preview, and testimonials preview.

How to verify:
1. Run `npm run dev` (or `npm run preview` after build)
2. Visit http://localhost:4321/ -- verify homepage shows:
   - Hero section with badge, headline, gradient text, two CTA buttons
   - Services grid with at least 3 service cards
   - Tech stack grid with technology ICONS (not just text)
   - Portfolio preview with 2-3 project cards
   - Testimonials preview with 2-3 quote cards
   - Contact CTA section at bottom
3. Click each nav link (Portfolio, Team, Blog, Pricing, Contact) -- verify:
   - Each page loads with smooth transition (no full page reload flash)
   - PageHeader shows gradient title and subtitle
   - Placeholder content displays on inner pages
   - Contact page shows the contact form
4. Resize browser to mobile width (~375px):
   - Hamburger icon appears, desktop nav links disappear
   - Tap hamburger -- sidebar slides in from right with glass styling
   - Tap a link in sidebar -- navigates and sidebar closes
   - No horizontal scrollbar at any width
5. Check at tablet width (~768px):
   - Grids collapse appropriately (services, projects, tech)
   - Content is readable, no overlapping elements

Resume signal: Type "approved" or describe any issues to fix.
  </action>
  <verify>User confirms visual approval</verify>
  <done>User has approved the visual appearance and functionality of all 6 pages across mobile, tablet, and desktop viewports.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Homepage HTML contains devicon CDN references (tech icons)
3. Homepage HTML contains testimonial content
4. Homepage HTML contains project preview cards
5. All 6 routes generate valid HTML in dist/
6. No TypeScript or build errors
</verification>

<success_criteria>
- Homepage displays all 6 sections: Hero, Services, Tech Stack (with icons), Portfolio preview, Testimonials preview, Contact CTA
- Tech stack items show Devicon SVG icons, not just text labels
- Portfolio and testimonials previews show placeholder data (will be replaced with real data in Phase 2)
- All pages fully responsive at 375px, 768px, and 1200px
- Mobile sidebar navigation works (open, navigate, close)
- View Transitions provide smooth page navigation
- User approves visual checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-migration/01-02-SUMMARY.md`
</output>
