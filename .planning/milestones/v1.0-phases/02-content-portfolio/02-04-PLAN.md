---
phase: 02-content-portfolio
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pages/portfolio/index.astro
  - src/pages/portfolio/[...id].astro
  - src/components/PortfolioCard.astro
  - src/pages/index.astro
  - src/styles/global.css
autonomous: true
requirements: [PORT-01, PORT-02, PORT-04, PORT-05, PORT-06]

must_haves:
  truths:
    - "Portfolio page displays curated projects with name, description, languages, and last updated date"
    - "Clicking a portfolio project opens a detail page with full case study content"
    - "GitHub PAT is never exposed in client-side HTML or JavaScript bundles"
    - "Homepage project preview links to individual portfolio detail pages"
  artifacts:
    - path: "src/pages/portfolio/index.astro"
      provides: "Portfolio grid page querying portfolio collection"
      contains: "getCollection"
    - path: "src/pages/portfolio/[...id].astro"
      provides: "Portfolio detail pages with case study content"
      contains: "getStaticPaths"
    - path: "src/components/PortfolioCard.astro"
      provides: "Portfolio grid card with metadata"
  key_links:
    - from: "src/pages/portfolio/index.astro"
      to: "portfolio collection"
      via: "getCollection('portfolio')"
      pattern: "getCollection.*portfolio"
    - from: "src/pages/portfolio/[...id].astro"
      to: "src/data/portfolio/*.md"
      via: "getCollection('portfolioCaseStudies') or direct glob"
      pattern: "getStaticPaths|render"
    - from: "src/pages/index.astro"
      to: "portfolio collection"
      via: "getCollection('portfolio')"
      pattern: "getCollection.*portfolio"
---

<objective>
Build the portfolio system: grid page showing projects from GitHub API (via content collection), individual detail pages with case study Markdown content, and update homepage project preview to use collection data.

Purpose: Portfolio is the primary trust-building content. Projects fetched from GitHub demonstrate real work; detail pages with case studies provide depth for prospective clients.
Output: Portfolio grid page, project detail pages, PortfolioCard component, updated homepage project section.
</objective>

<execution_context>
@/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-portfolio/02-RESEARCH.md
@.planning/phases/02-content-portfolio/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: portfolio collection schema -->

portfolio schema:
```typescript
z.object({
  title: z.string(),
  description: z.string(),
  languages: z.array(z.string()),
  updatedAt: z.string().nullable(),
  screenshots: z.array(z.string()).default([]),
  caseStudySlug: z.string().nullable(),
})
```

<!-- Portfolio case study Markdown frontmatter -->
```yaml
title: string
client: string
industry: string
duration: string
techStack: string[]
```

<!-- Existing ProjectCard from Phase 1 -->
From src/components/ProjectCard.astro:
```typescript
interface Props {
  tag: string;
  title: string;
  description: string;
  delay?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PortfolioCard component and portfolio grid page</name>
  <files>
    src/components/PortfolioCard.astro,
    src/pages/portfolio/index.astro,
    src/pages/index.astro,
    src/styles/global.css
  </files>
  <action>
    Delete (or move) existing `src/pages/portfolio.astro` placeholder. Portfolio listing will live at `src/pages/portfolio/index.astro`.

    Create `src/components/PortfolioCard.astro`:
    - Props: title (string), description (string), languages (string[]), updatedAt (string | null), caseStudySlug (string | null), delay (string, optional)
    - Render a glass-panel card with:
      - Title as h3
      - Description paragraph (truncated to ~120 chars with ellipsis if long)
      - Languages as small tech-tag pills (similar to blog tag pills but perhaps a different accent color like secondary/pink)
      - Last updated date formatted as "Updated Mar 2026" (if updatedAt is not null)
      - "View Case Study" link pointing to `/portfolio/${caseStudySlug}` if caseStudySlug exists; otherwise "View Details" as disabled/muted text
    - Use reveal/delay pattern

    Create `src/pages/portfolio/index.astro`:
    - Import `getCollection` from `astro:content`, BaseLayout, PageHeader, PortfolioCard
    - Query: `const projects = await getCollection('portfolio')`
    - Render PageHeader with title="Portfolio", subtitle="A selection of our finest work"
    - Render a responsive grid of PortfolioCards
    - Grid: 3 columns desktop, 2 tablet, 1 mobile

    Update `src/pages/index.astro` homepage projects section:
    - Import `getCollection` and use portfolio collection instead of inline `projects` array
    - Query: `const allProjects = await getCollection('portfolio')` then take first 3 with `.slice(0, 3)`
    - Update the .map() to use `p.data.title`, `p.data.description`, `p.data.languages.join(' / ')` for the tag prop
    - Update ProjectCard links: change the hardcoded `/portfolio` href to `/portfolio/${p.data.caseStudySlug || p.id}`
    - Remove the old inline projects array

    Add portfolio grid styles to `src/styles/global.css`:
    - `.portfolio-grid`: CSS grid, 3 columns (auto-fit, minmax ~300px), responsive
    - `.tech-tag`: small pill for language tags (similar to `.tag-pill` but using secondary color)
    - `.updated-date`: muted small text for last updated info
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Portfolio grid page shows projects with title, description, languages, updated date; homepage projects section uses collection data instead of inline array</done>
</task>

<task type="auto">
  <name>Task 2: Create portfolio detail pages with case study content</name>
  <files>
    src/pages/portfolio/[...id].astro
  </files>
  <action>
    Create a separate content collection query for case studies OR query the portfolio case study Markdown files directly. The case study Markdown files are in `src/data/portfolio/` with frontmatter (title, client, industry, duration, techStack).

    Two approaches (pick the simpler one):
    Option A: Add a `portfolioCaseStudies` collection to content.config.ts with `glob({ pattern: '**/*.md', base: './src/data/portfolio' })` -- but this requires modifying Plan 01's file. Instead:
    Option B: Use Astro's `getCollection('blog')` pattern but for portfolio -- however the case studies are already loadable. If the portfolio case studies are defined as a collection in content.config.ts (Plan 01 should define this), query them. If NOT defined as a separate collection, add one.

    IMPORTANT: Check if `content.config.ts` from Plan 01 already defines a collection for portfolio case study Markdown files. If it only defines the GitHub API loader collection (named `portfolio`), create a SECOND collection for the Markdown case studies. Add it to content.config.ts:

    ```typescript
    const portfolioCaseStudies = defineCollection({
      loader: glob({ pattern: '**/*.md', base: './src/data/portfolio' }),
      schema: z.object({
        title: z.string(),
        client: z.string(),
        industry: z.string(),
        duration: z.string(),
        techStack: z.array(z.string()),
      }),
    });
    ```
    Add `portfolioCaseStudies` to the exports.

    Create `src/pages/portfolio/[...id].astro`:
    - Import `getCollection`, `render` from `astro:content`, BaseLayout
    - Define `getStaticPaths()` using the case studies collection
    - Render:
      - BaseLayout with title={caseStudy.data.title}
      - Article wrapper (max-width ~720px, centered, same as blog post layout)
      - Header: title as h1, client name, industry, duration as metadata row
      - Tech stack as pill tags
      - `<Content />` renders the Markdown body (challenge, approach, results)
      - "Back to Portfolio" link at bottom
    - Reuse `.prose` / `.blog-post` styles from Plan 03 for article typography (or define shared `.article-content` class)
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>3 portfolio detail pages generated at build time, each showing case study content rendered from Markdown with project metadata, navigable from portfolio grid</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds and generates portfolio pages
- Portfolio grid at /portfolio shows all projects with metadata
- Each case study at /portfolio/{id} renders Markdown content
- Homepage projects section uses collection data (no inline array)
- `grep -r "ghp_\|GITHUB_PAT" dist/` returns nothing (PAT not leaked)
</verification>

<success_criteria>
- Portfolio grid displays 3 projects with title, description, languages, updated date
- 3 case study detail pages generated with full content
- Homepage projects section driven by portfolio collection
- GitHub PAT never appears in built output
- All links between portfolio grid and detail pages work
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-portfolio/02-04-SUMMARY.md`
</output>
