---
phase: 02-content-portfolio
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pages/blog/index.astro
  - src/pages/blog/[...id].astro
  - src/components/BlogPostCard.astro
  - src/styles/global.css
autonomous: true
requirements: [BLOG-01, BLOG-02, BLOG-03, BLOG-04]

must_haves:
  truths:
    - "Blog listing page shows all non-draft posts with title, date, excerpt, read time, and tags"
    - "Clicking a blog post opens its full Markdown-rendered content with syntax-highlighted code blocks"
    - "Blog posts display their tags as labels"
  artifacts:
    - path: "src/pages/blog/index.astro"
      provides: "Blog listing page querying blog collection"
      contains: "getCollection"
    - path: "src/pages/blog/[...id].astro"
      provides: "Dynamic blog post pages via getStaticPaths"
      contains: "getStaticPaths"
    - path: "src/components/BlogPostCard.astro"
      provides: "Blog listing card with metadata"
  key_links:
    - from: "src/pages/blog/index.astro"
      to: "src/data/blog/*.md"
      via: "getCollection('blog')"
      pattern: "getCollection.*blog"
    - from: "src/pages/blog/[...id].astro"
      to: "src/data/blog/*.md"
      via: "getStaticPaths + render()"
      pattern: "getStaticPaths|render\\("
---

<objective>
Build the blog system: listing page with post cards showing metadata, individual post pages rendered from Markdown with syntax highlighting and tag display.

Purpose: Blog is a key content marketing asset. The listing page drives discovery; individual posts demonstrate technical expertise via syntax-highlighted code blocks.
Output: Blog listing page, dynamic blog post pages, BlogPostCard component.
</objective>

<execution_context>
@/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-portfolio/02-RESEARCH.md
@.planning/phases/02-content-portfolio/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: blog collection schema -->

blog schema:
```typescript
z.object({
  title: z.string(),
  date: z.coerce.date(),
  excerpt: z.string(),
  tags: z.array(z.string()).default([]),
  draft: z.boolean().default(false),
})
```

<!-- Querying pattern from research -->
```typescript
import { getCollection, render } from 'astro:content';
const posts = await getCollection('blog', ({ data }) => !data.draft);
// Sort: newest first
const sorted = posts.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
// Render individual post:
const { Content } = await render(post);
```

<!-- Existing layout/components -->
From src/layouts/BaseLayout.astro: accepts title, description props
From src/components/PageHeader.astro: accepts title, subtitle props
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BlogPostCard component and blog listing page</name>
  <files>
    src/components/BlogPostCard.astro,
    src/pages/blog/index.astro,
    src/styles/global.css
  </files>
  <action>
    Delete (or move) the existing `src/pages/blog.astro` placeholder file. The blog listing will live at `src/pages/blog/index.astro` instead.

    Create `src/components/BlogPostCard.astro`:
    - Props: title (string), date (Date), excerpt (string), readTime (string), tags (string[]), href (string), delay (string, optional)
    - Render a glass-panel card with:
      - Date formatted as "Mar 1, 2026" using `date.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })`
      - Read time next to date (e.g., "Mar 1, 2026 -- 3 min read")
      - Title as h3 link to href
      - Excerpt paragraph (truncated to ~150 chars if needed)
      - Tags rendered as small pill/badge elements (styled inline spans with rounded background using primary color at low opacity)
    - Use reveal/delay pattern

    Create `src/pages/blog/index.astro`:
    - Import `getCollection` from `astro:content`, BaseLayout, PageHeader, BlogPostCard
    - Query: `const posts = await getCollection('blog', ({ data }) => !data.draft)`
    - Sort newest first: `.sort((a, b) => b.data.date.getTime() - a.data.date.getTime())`
    - Calculate read time: `Math.ceil((post.body || '').split(/\s+/).length / 200)` min read
    - Render PageHeader with title="Blog", subtitle="Insights, tutorials, and engineering stories"
    - Render a grid/list of BlogPostCards with href={`/blog/${post.id}`}
    - Layout: single column or 2-column grid on desktop, 1-column mobile

    Add blog styles to `src/styles/global.css`:
    - `.blog-grid`: layout for post cards (1 or 2 columns)
    - `.tag-pill`: small rounded badge (background: rgba of primary, padding, border-radius, small font)
    - `.tags-list`: flex wrap container for tag pills
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Blog listing page at /blog shows 3 sample posts sorted by date with title, date, excerpt, read time, and tag pills</done>
</task>

<task type="auto">
  <name>Task 2: Create dynamic blog post pages with Markdown rendering</name>
  <files>
    src/pages/blog/[...id].astro,
    src/styles/global.css
  </files>
  <action>
    Create `src/pages/blog/[...id].astro`:
    - Import `getCollection`, `render` from `astro:content`, BaseLayout
    - Define `getStaticPaths()`:
      ```typescript
      export async function getStaticPaths() {
        const posts = await getCollection('blog', ({ data }) => !data.draft);
        return posts.map((post) => ({
          params: { id: post.id },
          props: { post },
        }));
      }
      ```
    - Get post from props, render content: `const { Content } = await render(post)`
    - Calculate read time from post.body
    - Layout structure:
      - BaseLayout with title={post.data.title}
      - Article wrapper with max-width (~720px) centered
      - Post header: title as h1, date + read time, tags as pills
      - `<Content />` component renders the Markdown body
      - "Back to blog" link at bottom

    Add blog post (article/prose) styles to `src/styles/global.css`:
    - `.blog-post` or `.prose` class for article content: set line-height 1.8, paragraph spacing, heading sizes (h2, h3), link colors, list styles
    - Code blocks: ensure Shiki output integrates with dark theme (pre/code background should be dark, border-radius, padding, overflow-x auto)
    - Inline code: subtle background highlight
    - Blockquote styling: left border with primary color
    - Image max-width: 100%
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Individual blog post pages render Markdown with syntax-highlighted code blocks, display tags, show date and read time, build generates static pages for each post</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds and generates blog pages
- Blog listing at /blog shows all non-draft posts
- Each post page at /blog/{id} renders full Markdown content
- Code blocks have syntax highlighting (Shiki with github-dark theme)
- Tags display on both listing cards and individual post pages
</verification>

<success_criteria>
- 3 blog post pages generated at build time
- Blog listing sorted newest first with date, excerpt, read time, tags
- Code blocks in posts render with syntax highlighting colors (not plain text)
- Tags visible as styled pills/badges
- Responsive layout works on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-portfolio/02-03-SUMMARY.md`
</output>
