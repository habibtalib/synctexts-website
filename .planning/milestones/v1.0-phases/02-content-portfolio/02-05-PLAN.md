---
phase: 02-content-portfolio
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified: []
autonomous: false
requirements: [PORT-01, PORT-02, PORT-05, TEAM-01, TEST-01, PRIC-01, PRIC-02, BLOG-01, BLOG-03]

must_haves:
  truths:
    - "All content pages render correctly with glassmorphism design consistency"
    - "All data is driven by config files, not hardcoded HTML"
    - "Syntax highlighting renders in blog code blocks"
    - "All inter-page links work (portfolio grid to detail, blog listing to post)"
---

<objective>
Visual verification of all Phase 2 content pages across desktop and mobile viewports.

Purpose: Confirm that all data-driven pages render correctly, design is consistent, and inter-page navigation works before marking the phase complete.
Output: User approval of all content pages.
</objective>

<execution_context>
@/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and verify PAT security</name>
  <files></files>
  <action>
    Run `npm run build` to verify all collections, schemas, and pages build successfully. Then start `npm run preview` for the user to inspect.

    After build succeeds, verify PAT security:
    - Run `grep -r "ghp_" dist/ 2>/dev/null` to confirm no PAT in output
    - Run `grep -r "GITHUB_PAT" dist/ 2>/dev/null` to confirm env var name not leaked

    Report build success and any warnings to user.
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build succeeds with zero errors, PAT not present in dist/ output</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all content pages</name>
  <action>
    User visually inspects all Phase 2 content pages for correctness, design consistency, and functionality.

    What was built: Team, Pricing, Blog (listing + posts), Portfolio (grid + detail), updated homepage (testimonials + projects from collections).

    Verification steps with preview server running at http://localhost:4321:

    1. Homepage (http://localhost:4321/): Scroll to Projects section -- verify 3 project cards from portfolio collection. Scroll to Testimonials -- verify client quotes from collection data. Check "View All Projects" link goes to /portfolio.

    2. Team page (http://localhost:4321/team): Verify 3-4 team member cards with name, role, bio. Check responsive layout at mobile width.

    3. Pricing page (http://localhost:4321/pricing): Verify 3 pricing tiers (Starter, Growth, Enterprise). Check Growth tier has "Most Popular" highlight. Verify features list and CTA buttons. Tiers should stack on mobile.

    4. Blog listing (http://localhost:4321/blog): Verify 3 blog posts with title, date, excerpt, read time, tags. Posts sorted newest first.

    5. Blog post (click any post): Verify full Markdown content renders. Check code blocks have syntax highlighting. Verify tags display. Check "Back to blog" link.

    6. Portfolio grid (http://localhost:4321/portfolio): Verify 3 project cards with title, description, language tags. Click "View Case Study".

    7. Portfolio detail (any case study page): Verify case study content renders. Check project metadata. Check "Back to Portfolio" link.

    8. Mobile check: Resize to ~375px width, spot-check any 2 pages.
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User approves visual/functional state of all Phase 2 content pages</done>
</task>

</tasks>

<verification>
- All 6 content pages render without errors
- Design consistency maintained (glassmorphism, dark theme, typography)
- All inter-page links functional
- Syntax highlighting visible in blog code blocks
- Responsive layout works on mobile viewports
</verification>

<success_criteria>
- User approves visual/functional state of all content pages
- Build passes with zero errors
- No PAT leakage in built output
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-portfolio/02-05-SUMMARY.md`
</output>
