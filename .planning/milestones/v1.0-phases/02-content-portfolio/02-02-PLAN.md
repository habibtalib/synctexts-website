---
phase: 02-content-portfolio
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pages/team.astro
  - src/components/TeamMemberCard.astro
  - src/pages/pricing.astro
  - src/components/PricingTier.astro
  - src/pages/index.astro
  - src/styles/global.css
autonomous: true
requirements: [TEAM-01, TEAM-02, TEST-01, TEST-02, PRIC-01, PRIC-02, PRIC-03]

must_haves:
  truths:
    - "Team page displays member profiles with photo, name, role, and bio from YAML config"
    - "Homepage testimonials section renders quotes from content collection instead of inline data"
    - "Pricing page shows 3 service tiers with features, prices, and CTAs from YAML config"
  artifacts:
    - path: "src/pages/team.astro"
      provides: "Team listing page querying team collection"
      contains: "getCollection"
    - path: "src/components/TeamMemberCard.astro"
      provides: "Team member profile card component"
    - path: "src/pages/pricing.astro"
      provides: "Pricing page querying pricing collection"
      contains: "getCollection"
    - path: "src/components/PricingTier.astro"
      provides: "Pricing tier card component"
    - path: "src/pages/index.astro"
      provides: "Homepage with testimonials from collection"
      contains: "getCollection"
  key_links:
    - from: "src/pages/team.astro"
      to: "src/data/team.yaml"
      via: "getCollection('team')"
      pattern: "getCollection.*team"
    - from: "src/pages/pricing.astro"
      to: "src/data/pricing.yaml"
      via: "getCollection('pricing')"
      pattern: "getCollection.*pricing"
    - from: "src/pages/index.astro"
      to: "src/data/testimonials.yaml"
      via: "getCollection('testimonials')"
      pattern: "getCollection.*testimonials"
---

<objective>
Build the Team, Pricing, and Testimonials pages/sections using content collections. Replace placeholder pages and inline data with data-driven components querying YAML config files.

Purpose: Three content pages share the same pattern (query collection, render cards). Building them together maximizes reuse and consistency.
Output: Working Team page, Pricing page, and homepage testimonials section -- all driven by YAML config files.
</objective>

<execution_context>
@/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-portfolio/02-RESEARCH.md
@.planning/phases/02-content-portfolio/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: content.config.ts collection schemas -->

team schema:
```typescript
z.object({
  id: z.string(),
  name: z.string(),
  role: z.string(),
  bio: z.string(),
  photo: z.string(),
  order: z.number().default(0),
})
```

testimonials schema:
```typescript
z.object({
  id: z.string(),
  quote: z.string(),
  name: z.string(),
  role: z.string(),
  company: z.string(),
})
```

pricing schema:
```typescript
z.object({
  id: z.string(),
  name: z.string(),
  description: z.string(),
  price: z.string(),
  features: z.array(z.string()),
  highlighted: z.boolean().default(false),
  cta: z.string().default("Get Started"),
})
```

<!-- Existing components -->
From src/components/TestimonialCard.astro:
```typescript
interface Props {
  quote: string; name: string; role: string; company: string; delay?: string;
}
```

From src/components/PageHeader.astro:
```typescript
interface Props { title: string; subtitle?: string; }
```

From src/layouts/BaseLayout.astro:
- Accepts title and description props
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Team page and update homepage testimonials to use collections</name>
  <files>
    src/components/TeamMemberCard.astro,
    src/pages/team.astro,
    src/pages/index.astro,
    src/styles/global.css
  </files>
  <action>
    Create `src/components/TeamMemberCard.astro`:
    - Props: name (string), role (string), bio (string), photo (string), delay (string, optional)
    - Render a glass-panel card with: photo placeholder div (use CSS background or img tag with alt text), name as h3, role styled like a subtitle, bio paragraph
    - Use the same reveal/delay pattern as other cards (class="reveal", style with --delay)
    - Style the photo area as a circular or rounded-square avatar placeholder (~120px)

    Update `src/pages/team.astro`:
    - Import `getCollection` from `astro:content` and `TeamMemberCard`
    - Query: `const members = await getCollection('team')` then sort by `data.order`
    - Replace "Content coming soon" with a responsive grid of TeamMemberCards
    - Keep existing PageHeader with title="Our Team"
    - Grid layout: 2 columns on desktop, 1 on mobile (use CSS grid in global.css or scoped styles)

    Update `src/pages/index.astro`:
    - Import `getCollection` from `astro:content`
    - Replace the inline `testimonials` array with: `const testimonials = await getCollection('testimonials')`
    - Update the .map() to use `t.data.quote`, `t.data.name`, `t.data.role`, `t.data.company`
    - Add delay calculation: `delay={String((i + 1) * 0.1) + 's'}` using map index
    - Remove the old inline testimonials array entirely

    Add team grid styles to `src/styles/global.css`:
    - `.team-grid`: CSS grid, 2 columns on desktop, 1 on mobile, gap similar to other grids
    - Team card avatar placeholder styles
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Team page shows member cards from YAML, homepage testimonials load from collection instead of inline data, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Build Pricing page with tier cards</name>
  <files>
    src/components/PricingTier.astro,
    src/pages/pricing.astro,
    src/styles/global.css
  </files>
  <action>
    Create `src/components/PricingTier.astro`:
    - Props: name (string), description (string), price (string), features (string[]), highlighted (boolean), cta (string), delay (string, optional)
    - Render a glass-panel card with: tier name as h3 with text-gradient, price displayed prominently (large font), description paragraph, features as a bulleted list with checkmark icons (use ✓ character), CTA button linking to /contact
    - If `highlighted` is true, add a "Most Popular" badge and a subtle border-glow effect using the primary/secondary gradient (e.g., `border: 1px solid var(--primary)` or a box-shadow glow)
    - Use reveal/delay pattern

    Update `src/pages/pricing.astro`:
    - Import `getCollection` from `astro:content` and `PricingTier`
    - Query: `const tiers = await getCollection('pricing')`
    - Replace "Content coming soon" with a responsive grid of PricingTier cards
    - Keep existing PageHeader
    - Grid: 3 columns on desktop, 1 on mobile

    Add pricing styles to `src/styles/global.css`:
    - `.pricing-grid`: CSS grid, 3 columns on desktop (auto-fit, minmax ~320px), gap
    - Highlighted tier glow/border styles
    - Price display: large font, text-gradient
    - Feature list: clean spacing, checkmark bullets
    - CTA button: reuse existing button styles (.btn or similar)
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Pricing page shows 3 tiers with features, prices, CTAs from YAML config, highlighted tier has visual emphasis, build succeeds</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Team page renders member cards (not "coming soon")
- Pricing page renders 3 tier cards (not "coming soon")
- Homepage testimonials come from collection (no inline data array in index.astro)
- All data changes happen in YAML files, not in page templates
</verification>

<success_criteria>
- Team page shows 3-4 member profiles sorted by order field
- Pricing page shows 3 tiers with the Growth tier visually highlighted
- Homepage testimonials section unchanged in appearance but data-driven via getCollection
- No hardcoded content data in any .astro page file (all from collections)
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-portfolio/02-02-SUMMARY.md`
</output>
