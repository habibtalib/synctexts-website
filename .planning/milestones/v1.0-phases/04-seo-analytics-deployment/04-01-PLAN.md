---
phase: 04-seo-analytics-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - src/components/SEO/JsonLdOrganization.astro
  - src/components/SEO/JsonLdBlogPost.astro
  - src/pages/blog/[...id].astro
  - src/pages/index.astro
  - src/pages/team.astro
  - src/pages/pricing.astro
  - src/pages/contact.astro
  - src/pages/blog/index.astro
  - src/pages/portfolio/index.astro
  - src/pages/portfolio/[...id].astro
  - astro.config.mjs
  - public/og-default.png
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-06
  - SEO-07

must_haves:
  truths:
    - "Every page has a unique meta title and meta description in the HTML head"
    - "Every page has Open Graph tags (og:title, og:description, og:image, og:url, og:type) in the HTML head"
    - "Every page has a canonical URL link tag"
    - "sitemap-index.xml is auto-generated at build time and excludes /admin and /api routes"
    - "All pages include Organization JSON-LD structured data"
    - "Blog post pages include BlogPosting JSON-LD structured data"
    - "All pages use semantic HTML with proper heading hierarchy and landmark elements"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Extended SEO props (image, type, canonicalUrl, jsonLd), OG meta tags, canonical link"
      contains: "og:title"
    - path: "src/components/SEO/JsonLdOrganization.astro"
      provides: "Organization schema JSON-LD"
      contains: "application/ld+json"
    - path: "src/components/SEO/JsonLdBlogPost.astro"
      provides: "BlogPosting schema JSON-LD"
      contains: "BlogPosting"
    - path: "astro.config.mjs"
      provides: "Sitemap integration with filter"
      contains: "@astrojs/sitemap"
    - path: "public/og-default.png"
      provides: "Default OG image for social sharing"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/SEO/JsonLdOrganization.astro"
      via: "component import"
      pattern: "JsonLdOrganization"
    - from: "src/pages/blog/[...id].astro"
      to: "src/components/SEO/JsonLdBlogPost.astro"
      via: "component import in blog post template"
      pattern: "JsonLdBlogPost"
    - from: "astro.config.mjs"
      to: "sitemap-index.xml"
      via: "@astrojs/sitemap integration"
      pattern: "sitemap"
---

<objective>
Add comprehensive SEO foundation to the site: meta tags, Open Graph, canonical URLs, JSON-LD structured data, auto-generated sitemap, and semantic HTML audit.

Purpose: Make the site discoverable by search engines and render correctly when shared on social media.
Output: Extended BaseLayout with SEO props, JSON-LD components, sitemap integration, OG default image, semantic HTML improvements.
</objective>

<execution_context>
@/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-analytics-deployment/04-RESEARCH.md
@.planning/phases/04-seo-analytics-deployment/04-CONTEXT.md

@src/layouts/BaseLayout.astro
@astro.config.mjs
@src/pages/blog/[...id].astro

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title: string;
  description?: string;
}
```
Currently uses ClientRouter (View Transitions), imports global.css, BackgroundGlows, Navigation, Footer.
Title renders as: `{title} | SyncTexts`
Description has default: 'SyncTexts is a leading tech agency specializing in Web Development, DevOps, and Data Analytics.'

From astro.config.mjs:
```js
site: 'https://synctexts.com'  // Already set -- needed for canonical URLs and sitemap
adapter: node({ mode: 'standalone' })
```

From src/pages/blog/[...id].astro:
```astro
// Passes title and description to BaseLayout
<BaseLayout title={post.data.title} description={post.data.excerpt}>
// Has access to: post.data.title, post.data.excerpt, post.data.date, post.data.tags, post.body
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BaseLayout with OG tags, canonical URL, JSON-LD, and sitemap integration</name>
  <files>
    src/layouts/BaseLayout.astro,
    src/components/SEO/JsonLdOrganization.astro,
    src/components/SEO/JsonLdBlogPost.astro,
    astro.config.mjs,
    public/og-default.png
  </files>
  <action>
1. Install @astrojs/sitemap: `npx astro add sitemap --yes`. Then manually verify astro.config.mjs has the sitemap integration with a filter excluding /admin and /api routes:
   ```js
   sitemap({ filter: (page) => !page.includes('/admin') && !page.includes('/api') })
   ```

2. Create `public/og-default.png` -- a 1200x630 branded OG image. Since we cannot generate images, create a simple SVG-based placeholder and convert it, OR use a solid dark background (#0a0a0c) with "SyncTexts" text. The simplest approach: create a minimal HTML file, screenshot it, or just create a placeholder PNG. Use whatever CLI tool is available (e.g., create a simple 1200x630 solid color PNG using ImageMagick if installed, or place a TODO comment and create a minimal valid PNG).

3. Create `src/components/SEO/JsonLdOrganization.astro`:
   - Renders `<script type="application/ld+json">` with Organization schema
   - Fields: @context, @type "Organization", name "SyncTexts", url "https://synctexts.com", logo "https://synctexts.com/favicon.svg", description, sameAs (placeholder array: github.com/synctexts, linkedin.com/company/synctexts)
   - Use `set:html={JSON.stringify(orgSchema)}` to avoid HTML escaping

4. Create `src/components/SEO/JsonLdBlogPost.astro`:
   - Accept props: title, description, date (Date), author (string, default "SyncTexts Team"), url (string), image (string, optional)
   - Renders BlogPosting schema with: headline, description, datePublished (ISO string), author (Person), publisher (Organization with logo), mainEntityOfPage, image
   - Use `set:html={JSON.stringify(schema)}`

5. Extend `src/layouts/BaseLayout.astro` Props interface:
   ```typescript
   interface Props {
     title: string;
     description?: string;
     image?: string;        // OG image path, defaults to '/og-default.png'
     type?: string;         // og:type, defaults to 'website'
     canonicalUrl?: string; // Override canonical URL
     jsonLd?: object;       // Additional JSON-LD (unused for now, extensibility)
   }
   ```
   Add to `<head>` section (after existing meta tags, before ClientRouter):
   - `<link rel="canonical" href={canonical} />` where canonical = canonicalUrl || new URL(Astro.url.pathname, Astro.site).href
   - OG meta tags: og:title (use full title with " | SyncTexts"), og:description, og:image (absolute URL), og:url (canonical), og:type, og:site_name "SyncTexts"
   - Twitter card meta tags: twitter:card "summary_large_image", twitter:title, twitter:description, twitter:image
   - Import and render `<JsonLdOrganization />` on all pages (inside head)

  All new props have defaults so existing pages work without changes.
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -20 && echo "---" && grep -c 'og:title' dist/client/index.html && grep -c 'application/ld+json' dist/client/index.html && ls dist/client/sitemap-index.xml</automated>
  </verify>
  <done>Build succeeds. index.html contains og:title meta tag, application/ld+json script. sitemap-index.xml exists in dist/client/. All existing pages still render without passing new props.</done>
</task>

<task type="auto">
  <name>Task 2: Add BlogPosting JSON-LD to blog posts, unique meta per page, and semantic HTML audit</name>
  <files>
    src/pages/blog/[...id].astro,
    src/pages/index.astro,
    src/pages/team.astro,
    src/pages/pricing.astro,
    src/pages/contact.astro,
    src/pages/blog/index.astro,
    src/pages/portfolio/index.astro,
    src/pages/portfolio/[...id].astro
  </files>
  <action>
1. Update `src/pages/blog/[...id].astro`:
   - Import JsonLdBlogPost component
   - Render `<JsonLdBlogPost>` inside the page (after BaseLayout opening tag, as a child), passing title, description (excerpt), date, url (canonical for this post), author ("SyncTexts Team")
   - Actually, since JSON-LD should be in `<head>`, pass it through BaseLayout. Better approach: render the JsonLdBlogPost component inside the page content area (it outputs a script tag which browsers handle fine outside head too, and Astro will place it correctly). OR, pass a jsonLd prop to BaseLayout and render it there. Use the simpler approach: render `<JsonLdBlogPost ... />` directly in the blog post template body -- browsers accept `<script type="application/ld+json">` anywhere in the document.

2. Ensure every page passes unique, descriptive title and description to BaseLayout. Review each page and update if needed:
   - `index.astro`: title="Web Development, DevOps & Analytics Agency", description about SyncTexts services
   - `team.astro`: title="Our Team", description about the team
   - `pricing.astro`: title="Pricing", description about service tiers
   - `contact.astro`: title="Contact Us", description about getting in touch
   - `blog/index.astro`: title="Blog", description about insights and articles
   - `portfolio/index.astro`: title="Portfolio", description about projects
   - `portfolio/[...id].astro`: title from project name, description from project description
   - `blog/[...id].astro`: already passes post.data.title and post.data.excerpt (good)

3. Semantic HTML audit across all pages:
   - Verify each page has exactly one `<h1>` (BaseLayout wraps content in `<main>`, Navigation is in `<nav>`, Footer in `<footer>` -- good landmarks)
   - Check heading hierarchy: h1 -> h2 -> h3, no skips
   - Ensure sections use appropriate landmark elements where missing
   - Fix any heading hierarchy issues found (e.g., if a page jumps from h1 to h3)
   - Add `role` attributes only where semantic elements aren't available
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -5 && echo "--- Unique titles check ---" && for f in dist/client/index.html dist/client/team/index.html dist/client/pricing/index.html dist/client/contact/index.html dist/client/blog/index.html dist/client/portfolio/index.html; do grep -oP '(?<=<title>).*?(?=</title>)' "$f" 2>/dev/null; done && echo "--- Blog post JSON-LD ---" && ls dist/client/blog/*/index.html 2>/dev/null | head -1 | xargs grep -c 'BlogPosting' 2>/dev/null</automated>
  </verify>
  <done>Build succeeds. Every page has a unique title in the built HTML. Blog post pages contain BlogPosting JSON-LD. All pages have proper heading hierarchy (one h1, no level skips) and semantic landmark elements (nav, main, footer).</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- Every built HTML page in dist/client/ contains: og:title, og:description, og:image, og:url meta tags
- Every built HTML page contains canonical link tag
- Every built HTML page contains Organization JSON-LD
- Blog post pages additionally contain BlogPosting JSON-LD
- sitemap-index.xml exists and does NOT contain /admin or /api routes
- Every page has a unique `<title>` value
- No heading hierarchy violations (h1 present on each page, no level skips)
</verification>

<success_criteria>
1. All 8+ pages have unique meta titles and descriptions (SEO-01)
2. OG tags render on all pages with default image fallback (SEO-02)
3. sitemap-index.xml auto-generated excluding admin/API routes (SEO-03)
4. Organization JSON-LD on all pages, BlogPosting on blog posts (SEO-06)
5. Semantic HTML with proper heading hierarchy and landmarks throughout (SEO-07)
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-analytics-deployment/04-01-SUMMARY.md`
</output>
