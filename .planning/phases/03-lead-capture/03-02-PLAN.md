---
phase: 03-lead-capture
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/pages/admin/index.astro
  - src/pages/api/admin/toggle-read.ts
autonomous: false
requirements:
  - LEAD-03

must_haves:
  truths:
    - "Agency can view all contact form submissions at /admin"
    - "Admin page is protected by HTTP Basic Auth (only accessible with correct username/password)"
    - "Each submission shows date, name, email, company, full message, IP, read/unread status, and rate-limited flag"
    - "Agency can toggle read/unread status on submissions"
  artifacts:
    - path: "src/pages/admin/index.astro"
      provides: "Admin submissions list page with Basic Auth"
      contains: "prerender = false"
    - path: "src/pages/api/admin/toggle-read.ts"
      provides: "API endpoint to toggle read status"
      exports: ["POST"]
  key_links:
    - from: "src/pages/admin/index.astro"
      to: "src/db/index.ts"
      via: "db.select() query for submissions"
      pattern: "db\\.select"
    - from: "src/pages/api/admin/toggle-read.ts"
      to: "src/db/index.ts"
      via: "db.update() to toggle read flag"
      pattern: "db\\.update"
---

<objective>
Build a Basic Auth protected admin page at /admin that displays all contact form submissions with full details and allows toggling read/unread status.

Purpose: Agency needs to view and manage incoming leads without direct database access. This completes the lead capture loop -- form submits, email notifies, admin page provides a persistent record.
Output: Working admin page with submission list and read/unread toggle.
</objective>

<execution_context>
@/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lead-capture/03-RESEARCH.md
@.planning/phases/03-lead-capture/03-01-SUMMARY.md

@src/styles/global.css

<interfaces>
<!-- From Plan 01 -- database layer -->
From src/db/schema.ts:
```typescript
export const submissions = sqliteTable('submissions', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  name: text('name').notNull(),
  email: text('email').notNull(),
  company: text('company'),
  message: text('message').notNull(),
  ip: text('ip'),
  createdAt: text('created_at').notNull(),
  read: integer('read', { mode: 'boolean' }).notNull().default(false),
  rateLimited: integer('rate_limited', { mode: 'boolean' }).notNull().default(false),
});
```

From src/db/index.ts:
```typescript
export const db: DrizzleInstance; // singleton connection
```

<!-- Existing layout components -->
From src/layouts/BaseLayout.astro:
- Props: title, description
- Provides full page shell with nav, footer

From src/components/PageHeader.astro:
- Props: title, subtitle
- Renders page header with gradient styling
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin submissions page with Basic Auth and read toggle API</name>
  <files>
    src/pages/admin/index.astro,
    src/pages/api/admin/toggle-read.ts
  </files>
  <action>
    1. Create `src/pages/api/admin/toggle-read.ts`:
       - Add `export const prerender = false`
       - Export POST as APIRoute
       - Check Basic Auth header (same validation as admin page -- extract to a shared helper or inline)
       - Parse JSON body for `{ id: number }`
       - Query current read status: `db.select({ read: submissions.read }).from(submissions).where(eq(submissions.id, id)).get()`
       - Toggle: `db.update(submissions).set({ read: !current.read }).where(eq(submissions.id, id)).run()`
       - Return 200 with `{ success: true, read: !current.read }`
       - Return 401 if auth fails, 400 if id missing, 404 if not found

    2. Create `src/pages/admin/index.astro`:
       - Add `export const prerender = false` in frontmatter
       - Implement Basic Auth check in frontmatter:
         a. Get Authorization header from `Astro.request.headers`
         b. If missing or invalid: return `new Response('Unauthorized', { status: 401, headers: { 'WWW-Authenticate': 'Basic realm="Admin"' } })`
         c. Decode Base64, split user:pass, compare against `import.meta.env.ADMIN_USER` and `import.meta.env.ADMIN_PASS`
       - Query all submissions: `db.select().from(submissions).orderBy(desc(submissions.createdAt)).all()`
       - Use BaseLayout with title="Admin - Submissions"
       - Use PageHeader with title="Submissions" subtitle="Contact form submissions"
       - Render submission list in a container section:
         a. If no submissions: show empty state message in a glass-panel ("No submissions yet")
         b. For each submission: render a glass-panel card showing:
            - Date (formatted nicely from createdAt ISO string)
            - Name + Email (as a mailto: link)
            - Company (if present, otherwise omit)
            - Full message text
            - IP address (small, muted text)
            - Rate-limited badge (if rateLimited is true, show a small warning badge)
            - Read/Unread status indicator (colored dot or badge)
            - Toggle read button (small button that calls the toggle API)
         c. Unread submissions should have a visual distinction (e.g., left border highlight with primary color, or slightly different background)
       - Add inline script for toggle-read functionality:
         a. Add click handler on toggle buttons
         b. POST to `/api/admin/toggle-read` with submission id
         c. On success: update the visual indicator without page reload (toggle the dot/badge class and button text)
         d. Include Basic Auth header in the fetch request (extract from current page's auth context or re-prompt -- simplest: just reload the page after toggle, which re-authenticates via browser cached credentials)
         e. Simpler approach: make the toggle button a link/form that reloads the page. The browser caches Basic Auth credentials for the session so re-authentication is seamless.
       - Style the admin page using existing glass-panel, glass-input patterns. Add scoped styles for:
         a. Submission cards with readable layout
         b. Unread indicator (primary color left border or dot)
         c. Rate-limited badge (warning/orange styled)
         d. Responsive layout (cards stack on mobile)
         e. Metadata row for IP/date in smaller muted text
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Admin page at /admin protected by HTTP Basic Auth (prompts for credentials)
    - Invalid/missing credentials return 401 with WWW-Authenticate header
    - Submissions listed newest-first with all fields: date, name, email, company, message, IP, read/unread, rate-limited
    - Unread submissions visually distinct from read ones
    - Rate-limited submissions show a warning badge
    - Toggle read/unread works via button click
    - Empty state shown when no submissions exist
    - Page follows glassmorphism design system
    - Project builds without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify admin submissions page</name>
  <action>
    User verifies the admin page: Basic Auth protection, submission display, read/unread toggle, and responsive design.
  </action>
  <what-built>Admin page at /admin with HTTP Basic Auth protection, submission list showing all fields (date, name, email, company, message, IP, read/unread, rate-limited flag), and toggle read/unread functionality. Uses existing glassmorphism design system.</what-built>
  <how-to-verify>
    1. Ensure `.env` has `ADMIN_USER=admin` and `ADMIN_PASS=` set to a password of your choice
    2. Run `npm run dev`
    3. Visit http://localhost:4321/admin -- should prompt for username/password (browser native Basic Auth dialog)
    4. Enter wrong credentials -- should re-prompt (401)
    5. Enter correct credentials -- should show submissions page
    6. If no submissions exist, submit the contact form first at /contact, then revisit /admin
    7. Verify each submission card shows: date, name, email, company (if provided), full message, IP, read/unread indicator
    8. Click toggle read button -- submission should switch between read/unread state
    9. Verify rate-limited submissions (if any) show a distinct badge
    10. Check responsiveness: resize browser to mobile width, cards should stack properly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- /admin returns 401 without valid credentials
- /admin shows all submissions with correct credentials
- Submissions display all required fields
- Read/unread toggle works
- Rate-limited submissions visually flagged
- Page uses glassmorphism design system consistently
</verification>

<success_criteria>
- Admin page accessible only with correct Basic Auth credentials (ADMIN_USER + ADMIN_PASS env vars)
- All submissions visible with date, name, email, company, message, IP, read/unread, rate-limited flag
- Read/unread status can be toggled per submission
- Page is responsive and follows existing design system
</success_criteria>

<output>
After completion, create `.planning/phases/03-lead-capture/03-02-SUMMARY.md`
</output>
