---
phase: 02-content-portfolio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
  - src/data/team.yaml
  - src/data/testimonials.yaml
  - src/data/pricing.yaml
  - src/data/portfolio-config.yaml
  - src/data/blog/building-scalable-apis-with-nodejs.md
  - src/data/blog/kubernetes-zero-downtime-deployments.md
  - src/data/blog/flutter-state-management-guide.md
  - src/data/portfolio/project-crm.md
  - src/data/portfolio/project-infra.md
  - src/data/portfolio/project-fintech.md
  - astro.config.mjs
autonomous: true
requirements: [PORT-03, PORT-06, TEAM-02, TEST-02, PRIC-03, BLOG-04]

must_haves:
  truths:
    - "Astro build succeeds with all content collections defined and validated"
    - "All YAML data files pass Zod schema validation at build time"
    - "Blog Markdown files render with syntax highlighting enabled"
  artifacts:
    - path: "src/content.config.ts"
      provides: "All collection definitions (blog, team, testimonials, pricing, portfolio)"
      exports: ["collections"]
    - path: "src/data/team.yaml"
      provides: "Team member profile data"
    - path: "src/data/testimonials.yaml"
      provides: "Client testimonial data"
    - path: "src/data/pricing.yaml"
      provides: "Service tier data"
    - path: "src/data/portfolio-config.yaml"
      provides: "GitHub repo slugs and manual overrides"
    - path: "astro.config.mjs"
      provides: "Shiki syntax highlighting configuration"
  key_links:
    - from: "src/content.config.ts"
      to: "src/data/*.yaml"
      via: "file() loader paths"
      pattern: "file\\("
    - from: "src/content.config.ts"
      to: "src/data/blog/*.md"
      via: "glob() loader pattern"
      pattern: "glob\\("
---

<objective>
Create all content collection infrastructure: define every collection schema in content.config.ts, create all YAML config files with seed data, create sample blog posts with code blocks and tags, create portfolio case study Markdown files, and configure Shiki syntax highlighting.

Purpose: This is the foundation all other Phase 2 plans depend on. Content collections, data files, and Shiki config must exist before any page can query them.
Output: content.config.ts with 5 collections, 4 YAML files, 3 blog posts, 3 portfolio case studies, Shiki dark theme config.
</objective>

<execution_context>
@/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-portfolio/02-RESEARCH.md

<interfaces>
<!-- Existing component interfaces that data files must match -->

From src/components/TestimonialCard.astro:
```typescript
interface Props {
  quote: string;
  name: string;
  role: string;
  company: string;
  delay?: string;
}
```

From src/components/ProjectCard.astro:
```typescript
interface Props {
  tag: string;
  title: string;
  description: string;
  delay?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create content.config.ts with all collection schemas and all YAML/Markdown data files</name>
  <files>
    src/content.config.ts,
    src/data/team.yaml,
    src/data/testimonials.yaml,
    src/data/pricing.yaml,
    src/data/portfolio-config.yaml,
    src/data/portfolio/project-crm.md,
    src/data/portfolio/project-infra.md,
    src/data/portfolio/project-fintech.md
  </files>
  <action>
    Create `src/content.config.ts` defining ALL 5 collections:

    1. **blog** collection: `glob({ pattern: '**/*.md', base: './src/data/blog' })` loader. Schema: title (string), date (coerce.date), excerpt (string), tags (array of strings, default []), draft (boolean, default false).

    2. **team** collection: `file('./src/data/team.yaml')` loader. Schema: id (string), name (string), role (string), bio (string), photo (string), order (number, default 0).

    3. **testimonials** collection: `file('./src/data/testimonials.yaml')` loader. Schema: id (string), quote (string), name (string), role (string), company (string).

    4. **pricing** collection: `file('./src/data/pricing.yaml')` loader. Schema: id (string), name (string), description (string), price (string), features (array of strings), highlighted (boolean, default false), cta (string, default "Get Started").

    5. **portfolio** collection: Custom inline loader that reads `./src/data/portfolio-config.yaml`, fetches GitHub repo data via REST API using `process.env.GITHUB_PAT` (NOT import.meta.env), merges with local overrides, and returns entries. Must gracefully handle missing PAT (log warning, use only local data). Schema: title (string), description (string), languages (array of strings), updatedAt (string, nullable), screenshots (array of strings, default []), caseStudySlug (string, nullable).

    Use `import { defineCollection, z } from 'astro:content'` and `import { glob, file } from 'astro/loaders'`. Export all as `collections`.

    Create `src/data/team.yaml` with 3-4 team members. Each entry must have `id` field. Use realistic agency roles (Full-Stack Engineer, DevOps Lead, UI/UX Designer, etc.). Use `/images/team/{id}.webp` for photo paths (placeholder paths -- real images later).

    Create `src/data/testimonials.yaml` with the 3 existing testimonials from index.astro (Sarah Mitchell, James Rodriguez, Emily Chen) plus add 1-2 more. Each entry must have an `id` field.

    Create `src/data/pricing.yaml` with 3 tiers: Starter (~$2,500/mo), Growth (~$5,000/mo, highlighted: true), Enterprise (Custom pricing). Each with 5-7 features, a CTA label, and an `id` field.

    Create `src/data/portfolio-config.yaml` with 3 placeholder projects matching the existing homepage preview data (enterprise-crm, k8s-platform, fintech-wallet). Use placeholder `repo` fields like `synctexts/enterprise-crm`. Include manual overrides (title, description, techTags, caseStudySlug).

    Create 3 portfolio case study Markdown files in `src/data/portfolio/`:
    - `project-crm.md`: Enterprise CRM Dashboard case study (challenge, solution, results, tech stack)
    - `project-infra.md`: High-Availability Microservices case study
    - `project-fintech.md`: FinTech Mobile Wallet case study
    Each with frontmatter: title, client, industry, duration, techStack (array). Body: 200-300 words covering challenge, approach, results.
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>content.config.ts exports all 5 collections, all YAML files pass schema validation, portfolio case study Markdown files exist, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Create sample blog posts and configure Shiki syntax highlighting</name>
  <files>
    src/data/blog/building-scalable-apis-with-nodejs.md,
    src/data/blog/kubernetes-zero-downtime-deployments.md,
    src/data/blog/flutter-state-management-guide.md,
    astro.config.mjs
  </files>
  <action>
    Create 3 sample blog posts in `src/data/blog/` demonstrating Markdown features:

    1. `building-scalable-apis-with-nodejs.md`: Frontmatter: title, date (2026-02-15), excerpt (~100 chars), tags: ["Node.js", "API", "Backend"], draft: false. Body: 400-500 words about API design patterns. MUST include at least one TypeScript/JavaScript code block to test syntax highlighting.

    2. `kubernetes-zero-downtime-deployments.md`: Frontmatter: title, date (2026-01-20), excerpt, tags: ["Kubernetes", "DevOps", "CI/CD"], draft: false. Body: 400-500 words about zero-downtime deployment strategies. Include a YAML code block (k8s manifest snippet).

    3. `flutter-state-management-guide.md`: Frontmatter: title, date (2026-03-01), excerpt, tags: ["Flutter", "Mobile", "Dart"], draft: false. Body: 400-500 words about state management. Include a Dart code block.

    Update `astro.config.mjs` to configure Shiki dark theme:
    ```javascript
    export default defineConfig({
      site: 'https://synctexts.com',
      markdown: {
        shikiConfig: {
          theme: 'github-dark',
        },
      },
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>3 blog posts exist with code blocks and tags, Shiki configured with github-dark theme, build succeeds with all content validated</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds (validates all schemas, loaders, and Markdown rendering)
- `src/content.config.ts` defines blog, team, testimonials, pricing, portfolio collections
- All YAML files have `id` fields on every entry
- Blog posts have frontmatter with title, date, excerpt, tags fields
- Portfolio loader gracefully handles missing GITHUB_PAT
</verification>

<success_criteria>
- Build passes with zero schema validation errors
- 5 content collections defined and queryable via getCollection()
- 3 blog posts, 3-4 team members, 3-5 testimonials, 3 pricing tiers, 3 portfolio projects in data files
- Shiki syntax highlighting configured for dark theme
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-portfolio/02-01-SUMMARY.md`
</output>
