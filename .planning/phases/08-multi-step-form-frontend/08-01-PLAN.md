---
phase: 08-multi-step-form-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/contact.astro
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FORM-03
  - FORM-07

must_haves:
  truths:
    - "Step indicator with 3 numbered dots, connecting lines, and labels is visible above the form"
    - "Step 1 shows 3 clickable service cards (Web Development, DevOps, Analytics) in a glassmorphism grid"
    - "Step 2 shows budget and timeline select dropdowns with glass-input styling"
    - "Step 3 shows name, email, company, message fields with existing validation markup"
    - "Only one step panel is visible at a time (others have display:none)"
    - "CSS animation classes exist for horizontal slide transitions gated on prefers-reduced-motion"
    - "Service cards have hover glow + scale(1.02) effects matching existing glass-panel patterns"
  artifacts:
    - path: "src/pages/contact.astro"
      provides: "Multi-step form HTML structure with step indicator, 3 step panels, scoped CSS"
      contains: "data-step"
  key_links:
    - from: "src/pages/contact.astro"
      to: "src/scripts/contact-form.ts"
      via: "script tag reference"
      pattern: "src=\"../scripts/contact-form.ts\""
---

<objective>
Rewrite the contact.astro page to contain the multi-step form HTML structure and all scoped CSS for the 3-step wizard: step indicator, service selection cards (Step 1), project details selects (Step 2), contact info fields (Step 3), and success panel. All new CSS for step transitions, card states, indicator states, and responsive breakpoints goes in the scoped style block.

Purpose: Establish the complete DOM structure and visual design so that Plan 02 (TypeScript state machine) can wire interactivity without touching HTML or CSS.
Output: A fully styled but non-interactive multi-step form (Step 1 visible by default, other steps hidden).
</objective>

<execution_context>
@/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-step-form-frontend/08-CONTEXT.md
@.planning/phases/08-multi-step-form-frontend/08-RESEARCH.md
@.planning/phases/08-multi-step-form-frontend/08-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs from existing codebase -->

From src/lib/validation.ts — enum values to duplicate as HTML select options:
```typescript
export const ALLOWED_SERVICE_TYPES = ['web_dev', 'devops', 'analytics'] as const;
export const ALLOWED_BUDGETS = ['under_5k', '5k_15k', '15k_50k', '50k_plus'] as const;
export const ALLOWED_TIMELINES = ['asap', '1_3_months', '3_6_months', 'exploring'] as const;
```

From src/pages/api/contact.ts — POST body field names the form must match:
```typescript
// Required: name, email, message
// Optional: company, service_type, budget, timeline, website (honeypot)
```

From existing contact.astro — CSS classes already defined in global.css to reuse:
- `.glass-panel` — glassmorphism card
- `.glass-input` — form input styling
- `.btn`, `.btn-primary`, `.btn-lg`, `.btn-block` — button variants
- `.form-group` — form field wrapper
- `.text-gradient` — gradient text effect
- `.container`, `.section` — layout containers
- `.reveal` — scroll animation trigger

Existing scoped CSS classes to preserve (already in contact.astro style block):
- `.field-error` — inline error display
- `.form-errors` — error summary box
- `.input-error` — red border on invalid input
- `.spinner` — loading spinner
- `.success-panel` — post-submission success display
- `.success-icon` — green checkmark circle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite contact.astro HTML structure and scoped CSS for multi-step wizard</name>
  <files>src/pages/contact.astro</files>
  <read_first>
    - src/pages/contact.astro (current HTML structure and scoped CSS to preserve/extend)
    - src/components/ServiceCard.astro (hover effect reference for service selection cards)
    - src/styles/global.css (design tokens: --primary, --text-primary, --text-secondary, --text-dimmed, --glass-border, --glass-shadow, --primary-glow, --bg-base, --transition-smooth)
    - .planning/phases/08-multi-step-form-frontend/08-UI-SPEC.md (visual contract — spacing, typography, color, copy, layout)
    - .planning/phases/08-multi-step-form-frontend/08-RESEARCH.md (HTML patterns for step indicator, service cards, select styling)
    - .planning/phases/08-multi-step-form-frontend/08-CONTEXT.md (locked decisions)
  </read_first>
  <action>
Rewrite `src/pages/contact.astro` completely. Keep the frontmatter imports (BaseLayout, PageHeader) and the script tag reference to `../scripts/contact-form.ts`. Replace the form HTML and add all new scoped CSS.

**HTML Structure (inside `.contact-wrapper`):**

1. **Left column (`.contact-info`)** — keep existing: heading "Ready to Accelerate?" and paragraph. No changes.

2. **Right column — form area:** Replace the current `<form>` with this structure:

```
<div class="form-errors" id="form-errors" role="alert"></div>

<!-- Step indicator — hidden on success via JS adding .is-hidden -->
<div class="step-indicator" id="step-indicator" aria-label="Form progress">
  <div class="step-dot is-current" data-indicator-step="1" aria-current="step" aria-label="Step 1: Service">
    <span class="step-dot-number">1</span>
    <span class="step-dot-check">&#10003;</span>
  </div>
  <div class="step-line" data-indicator-line="1"></div>
  <div class="step-dot is-pending" data-indicator-step="2" aria-label="Step 2: Details">
    <span class="step-dot-number">2</span>
    <span class="step-dot-check">&#10003;</span>
  </div>
  <div class="step-line" data-indicator-line="2"></div>
  <div class="step-dot is-pending" data-indicator-step="3" aria-label="Step 3: Contact">
    <span class="step-dot-number">3</span>
    <span class="step-dot-check">&#10003;</span>
  </div>
</div>
<div class="step-labels" id="step-labels" aria-hidden="true">
  <span>Service</span>
  <span>Details</span>
  <span>Contact</span>
</div>

<form class="contact-form" id="contact-form">
  <!-- Step 1: Service Selection -->
  <div class="form-step is-active" data-step="1">
    <h3 class="form-step-heading">What can we help you with?</h3>
    <div class="service-select-grid">
      <button class="service-select-card glass-panel" data-service-value="web_dev" type="button">
        <span class="service-select-icon">&#128187;</span>
        <span class="service-select-name">Web Development</span>
        <span class="service-select-desc">Custom sites, apps, and e-commerce</span>
      </button>
      <button class="service-select-card glass-panel" data-service-value="devops" type="button">
        <span class="service-select-icon">&#9881;&#65039;</span>
        <span class="service-select-name">DevOps</span>
        <span class="service-select-desc">CI/CD, infrastructure, and deployment</span>
      </button>
      <button class="service-select-card glass-panel" data-service-value="analytics" type="button">
        <span class="service-select-icon">&#128202;</span>
        <span class="service-select-name">Analytics</span>
        <span class="service-select-desc">Data pipelines, dashboards, and insights</span>
      </button>
    </div>
  </div>

  <!-- Step 2: Project Details -->
  <div class="form-step" data-step="2">
    <h3 class="form-step-heading">Tell us about your project</h3>
    <div class="form-group">
      <label for="budget" class="form-label">Budget</label>
      <select id="budget" name="budget" class="glass-input">
        <option value="">Select budget range...</option>
        <option value="under_5k">Under $5K</option>
        <option value="5k_15k">$5K - $15K</option>
        <option value="15k_50k">$15K - $50K</option>
        <option value="50k_plus">$50K+</option>
      </select>
    </div>
    <div class="form-group">
      <label for="timeline" class="form-label">Timeline</label>
      <select id="timeline" name="timeline" class="glass-input">
        <option value="">Select timeline...</option>
        <option value="asap">ASAP</option>
        <option value="1_3_months">1-3 Months</option>
        <option value="3_6_months">3-6 Months</option>
        <option value="exploring">Just Exploring</option>
      </select>
    </div>
    <p class="form-helper">Both fields are optional — we'll discuss details on the call.</p>
    <div class="step-nav">
      <button type="button" class="step-back-link" data-back>&#8592; Back</button>
      <button type="button" class="btn btn-primary" data-next>Next: Contact Info</button>
    </div>
  </div>

  <!-- Step 3: Contact Info -->
  <div class="form-step" data-step="3">
    <h3 class="form-step-heading">How do we reach you?</h3>
    <div class="form-group">
      <label for="name" class="form-label">Name</label>
      <input type="text" id="name" name="name" required placeholder="Your name" class="glass-input">
      <span class="field-error" id="name-error"></span>
    </div>
    <div class="form-group">
      <label for="email" class="form-label">Email</label>
      <input type="email" id="email" name="email" required placeholder="your@email.com" class="glass-input">
      <span class="field-error" id="email-error"></span>
    </div>
    <div class="form-group">
      <label for="company" class="form-label">Company (optional)</label>
      <input type="text" id="company" name="company" placeholder="Company name" class="glass-input">
    </div>
    <div class="form-group">
      <label for="message" class="form-label">Project Details</label>
      <textarea id="message" name="message" required placeholder="Tell us more about your project..." class="glass-input" rows="4"></textarea>
      <span class="field-error" id="message-error"></span>
    </div>
    <!-- Honeypot field - hidden from humans -->
    <div style="position:absolute;left:-9999px;" aria-hidden="true">
      <input type="text" name="website" tabindex="-1" autocomplete="off">
    </div>
    <div class="step-nav">
      <button type="button" class="step-back-link" data-back>&#8592; Back</button>
      <button type="submit" class="btn btn-primary btn-block">Send Message</button>
    </div>
  </div>
</form>
```

**Scoped CSS (`<style>` block) — include ALL of the following:**

Preserve ALL existing scoped styles (`.field-error`, `.form-errors`, `.form-errors ul`, `.form-errors li`, `.input-error`, `.spinner`, `@keyframes spin`, `.success-panel`, `.success-icon`, `.success-panel h2`, `.success-panel p`, `button:disabled`) exactly as they are.

Add these NEW CSS rules:

**Step Indicator:**
- `.step-indicator`: `display: flex; align-items: center; justify-content: center; margin-bottom: 0.5rem;`
- `.step-indicator.is-hidden`: `display: none;`
- `.step-labels`: `display: flex; justify-content: center; gap: 0; margin-bottom: 2rem;`
- `.step-labels span`: `flex: 1; text-align: center; font-size: 0.75rem; color: var(--text-secondary, #94a3b8);`
- `.step-dot`: `width: 2.5rem; height: 2.5rem; border-radius: 50%; display: flex; align-items: center; justify-content: center; border: 2px solid var(--glass-border, rgba(255,255,255,0.1)); background: transparent; cursor: default; position: relative; flex-shrink: 0;`
- `.step-dot-number`: `font-family: 'Outfit', sans-serif; font-size: 0.875rem; font-weight: 600; color: var(--text-dimmed, #64748b);`
- `.step-dot-check`: `display: none; font-size: 0.875rem;`
- `.step-dot.is-current`: `background: var(--primary, #6366f1); border-color: var(--primary, #6366f1);`
- `.step-dot.is-current .step-dot-number`: `color: #ffffff;`
- `.step-dot.is-complete`: `background: rgba(99,102,241,0.2); border-color: var(--primary, #6366f1); cursor: pointer;`
- `.step-dot.is-complete .step-dot-number`: `display: none;`
- `.step-dot.is-complete .step-dot-check`: `display: block; color: var(--primary, #6366f1);`
- `.step-dot.is-complete:focus-visible`: `outline: 2px solid var(--primary, #6366f1); outline-offset: 2px;`
- `.step-line`: `flex: 1; height: 2px; background: var(--glass-border, rgba(255,255,255,0.1)); margin: 0 0.5rem; position: relative;`
- `.step-line.is-filled`: `background: var(--primary, #6366f1);`
- `@media (prefers-reduced-motion: no-preference)` block: `.step-line { transition: background 0.3s ease; }`

**Form Steps:**
- `.form-step`: `display: none;`
- `.form-step.is-active`: `display: block;`
- `.form-step-heading`: `font-family: 'Outfit', sans-serif; font-size: 1.25rem; font-weight: 600; margin-bottom: 1.5rem; color: var(--text-primary, #ffffff);`
- `@media (prefers-reduced-motion: no-preference)` block: `.form-step { transition: transform 0.3s ease, opacity 0.3s ease; }` and four directional classes:
  - `.form-step.enter-from-right { transform: translateX(40px); opacity: 0; }`
  - `.form-step.enter-from-left { transform: translateX(-40px); opacity: 0; }`
  - `.form-step.exit-to-left { transform: translateX(-40px); opacity: 0; }`
  - `.form-step.exit-to-right { transform: translateX(40px); opacity: 0; }`

**Service Cards:**
- `.service-select-grid`: `display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem;`
- `.service-select-card`: `display: flex; flex-direction: column; align-items: center; text-align: center; padding: 2rem; border-radius: 16px; cursor: pointer; border: 1px solid var(--glass-border, rgba(255,255,255,0.1)); background: rgba(255,255,255,0.03); transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1), border-color 0.5s cubic-bezier(0.16, 1, 0.3, 1), box-shadow 0.5s cubic-bezier(0.16, 1, 0.3, 1), background 0.3s ease;`
- `.service-select-card:hover`: `transform: scale(1.02); border-color: rgba(99,102,241,0.4); box-shadow: 0 0 20px rgba(99,102,241,0.2); background: rgba(255,255,255,0.06);`
- `.service-select-card.is-selected`: `transform: scale(1.02); border: 2px solid var(--primary, #6366f1); background: rgba(99,102,241,0.08); box-shadow: 0 0 24px rgba(99,102,241,0.3);`
- `.service-select-card:focus-visible`: `border-color: var(--primary, #6366f1); box-shadow: 0 0 20px rgba(99,102,241,0.2);`
- `.service-select-card:active`: `transform: scale(0.98);` (tap feedback)
- `.service-select-icon`: `font-size: 2.5rem; margin-bottom: 0.75rem;`
- `.service-select-name`: `font-family: 'Outfit', sans-serif; font-size: 1rem; font-weight: 600; color: var(--text-primary, #ffffff); margin-bottom: 0.25rem;`
- `.service-select-desc`: `font-size: 0.875rem; color: var(--text-secondary, #94a3b8); line-height: 1.5;`

**Select Dropdown Styling:**
- `select.glass-input`: `appearance: none; -webkit-appearance: none; background-image: url("data:image/svg+xml;charset=utf-8,%3Csvg xmlns='http://www.w3.org/2000/svg' width='16' height='16' fill='%2394a3b8'%3E%3Cpath d='M4 6l4 4 4-4'/%3E%3C/svg%3E"); background-repeat: no-repeat; background-position: right 1rem center; padding-right: 2.5rem; cursor: pointer; color-scheme: dark;`
- `select.glass-input option`: `background: #0a0a0c; color: #ffffff;`

**Form Labels and Helpers:**
- `.form-label`: `display: block; font-size: 0.875rem; font-weight: 600; color: var(--text-secondary, #94a3b8); margin-bottom: 0.5rem;`
- `.form-helper`: `font-size: 0.875rem; color: var(--text-dimmed, #64748b); margin-top: 0.5rem; margin-bottom: 1.5rem;`

**Step Navigation:**
- `.step-nav`: `display: flex; align-items: center; justify-content: space-between; margin-top: 1.5rem;`
- `.step-back-link`: `background: none; border: none; color: var(--text-secondary, #94a3b8); font-size: 0.875rem; cursor: pointer; padding: 0.5rem; font-family: 'Inter', sans-serif;`
- `.step-back-link:hover`: `color: var(--text-primary, #ffffff);`

**Responsive:**
- `@media (max-width: 600px)`: `.service-select-grid { grid-template-columns: 1fr; }`

**Success Panel Addition (append to existing `.success-panel` styles):**
- `.success-panel .btn`: `margin-top: 1.5rem;`
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - contact.astro contains `data-step="1"` and `data-step="2"` and `data-step="3"`
    - contact.astro contains `data-indicator-step="1"` and `data-indicator-step="2"` and `data-indicator-step="3"`
    - contact.astro contains `data-indicator-line="1"` and `data-indicator-line="2"`
    - contact.astro contains `data-service-value="web_dev"` and `data-service-value="devops"` and `data-service-value="analytics"`
    - contact.astro contains `class="service-select-card glass-panel"`
    - contact.astro contains `class="step-indicator"` and `class="step-labels"`
    - contact.astro contains `select.glass-input` in style block with `appearance: none`
    - contact.astro contains `.form-step.enter-from-right` in a `prefers-reduced-motion: no-preference` media query
    - contact.astro contains `.form-step.is-active` with `display: block`
    - contact.astro contains `name="website"` honeypot field preserved
    - contact.astro contains `aria-label="Form progress"` on step indicator
    - contact.astro contains existing `.field-error`, `.form-errors`, `.spinner`, `.success-panel`, `.success-icon` styles preserved
    - contact.astro contains `src="../scripts/contact-form.ts"` script reference preserved
    - contact.astro contains select options with values `under_5k`, `5k_15k`, `15k_50k`, `50k_plus`, `asap`, `1_3_months`, `3_6_months`, `exploring`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>contact.astro is fully rewritten with 3-step wizard HTML structure, step indicator, service cards, select fields, contact fields, honeypot, and all scoped CSS including animations, responsive breakpoints, and card states. Step 1 is visible by default, Steps 2-3 are hidden. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `grep -c 'data-step=' src/pages/contact.astro` returns 3 (three step panels)
- `grep -c 'data-indicator-step=' src/pages/contact.astro` returns 3 (three indicator dots)
- `grep -c 'data-service-value=' src/pages/contact.astro` returns 3 (three service cards)
- `grep 'prefers-reduced-motion' src/pages/contact.astro` matches at least once
</verification>

<success_criteria>
The contact page has complete multi-step form HTML with step indicator, 3 step panels (service cards, project details selects, contact fields), all scoped CSS for visual states, animations, and responsive breakpoints. Step 1 is visible, others hidden. Build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-step-form-frontend/08-01-SUMMARY.md`
</output>
