---
phase: 01-foundation-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - package.json
  - tsconfig.json
  - src/layouts/BaseLayout.astro
  - src/components/Navigation.astro
  - src/components/Footer.astro
  - src/components/BackgroundGlows.astro
  - src/components/PageHeader.astro
  - src/styles/global.css
  - src/scripts/animations.js
  - src/pages/index.astro
  - src/pages/portfolio.astro
  - src/pages/team.astro
  - src/pages/blog.astro
  - src/pages/pricing.astro
  - src/pages/contact.astro
  - public/favicon.svg
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05

must_haves:
  truths:
    - "Site runs as an Astro project and builds successfully with `npm run build`"
    - "All 6 routes generate HTML files in dist/ (index, portfolio, team, blog, pricing, contact)"
    - "Sticky navigation bar displays links to all 6 pages plus a CTA button"
    - "Mobile sidebar slides in from right with glassmorphism styling when hamburger is tapped"
    - "Glassmorphism design system (glass panels, gradient text, dark theme) renders on all pages"
    - "All pages share consistent layout shell (nav, footer, background glows)"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Astro project configuration with static output"
      contains: "defineConfig"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Shared layout shell with head, nav, footer, View Transitions"
      contains: "ClientRouter"
    - path: "src/components/Navigation.astro"
      provides: "Sticky nav with desktop links and mobile sidebar"
      contains: "hamburger"
    - path: "src/styles/global.css"
      provides: "Migrated design system with all CSS tokens and utilities"
      contains: "glass-panel"
    - path: "src/pages/index.astro"
      provides: "Homepage route"
      contains: "BaseLayout"
    - path: "src/pages/portfolio.astro"
      provides: "Portfolio placeholder page"
      contains: "PageHeader"
  key_links:
    - from: "src/pages/*.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "import and wrapping component"
      pattern: "import BaseLayout"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Navigation.astro"
      via: "component import and render"
      pattern: "import Navigation"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "CSS import in frontmatter"
      pattern: "import.*global\\.css"
---

<objective>
Initialize the Astro project, create the shared layout shell (BaseLayout with Navigation, Footer, BackgroundGlows), migrate the CSS design system, and scaffold all 6 page routes with placeholder content for inner pages.

Purpose: Establishes the multi-page Astro foundation that all subsequent work builds on -- routing, layout, navigation, and design tokens.
Output: A buildable Astro project with 6 routes, sticky nav with mobile sidebar, and the glassmorphism design system applied consistently.
</objective>

<execution_context>
@/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-migration/01-CONTEXT.md
@.planning/phases/01-foundation-migration/01-RESEARCH.md

@index.html
@style.css
@main.js

<interfaces>
<!-- Existing codebase to migrate FROM. Key patterns: -->

From style.css:
- `:root` block defines all CSS custom properties (colors, fonts, transitions)
- `.glass-panel` class with `backdrop-filter: blur(20px) saturate(180%)`
- `.text-gradient` utility with `background-clip: text`
- `.glass-nav` with `.scrolled` state for scroll-triggered compact mode
- `.btn` / `.btn-primary` / `.btn-secondary` / `.btn-lg` / `.btn-block` button system
- `.reveal` / `.reveal.active` for IntersectionObserver animations
- Responsive breakpoints at 900px and 600px

From main.js:
- Navbar scroll effect (add/remove `.scrolled` class)
- Mouse glow tracking effect
- IntersectionObserver for `.reveal` elements with `--delay` support
- Contact form simulated submission with loading/success states
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro project and migrate design system</name>
  <files>
    astro.config.mjs,
    package.json,
    tsconfig.json,
    src/styles/global.css,
    src/scripts/animations.js,
    public/favicon.svg
  </files>
  <action>
1. Initialize a new Astro project in the current directory. Since the project already has files, use `npm install astro` directly rather than `npm create astro`. Create `astro.config.mjs` with `defineConfig({ site: 'https://synctexts.com' })` -- no adapter, default static output per research.

2. Create `tsconfig.json` extending `astro/tsconfigs/strict`.

3. Update `package.json` scripts to:
   - `"dev": "astro dev"`
   - `"build": "astro build"`
   - `"preview": "astro preview"`

4. Create `src/styles/global.css` by migrating the full content of `style.css`. Keep ALL CSS custom properties, ALL utility classes (`.glass-panel`, `.text-gradient`, `.btn` variants, `.reveal`), ALL animation keyframes. Remove the nav-links `display: none` at 600px (will be replaced by mobile sidebar styles). Add new styles for:
   - `.mobile-sidebar` -- fixed right panel, glassmorphism styling, `transform: translateX(100%)` default, transitions to `translateX(0)` when open
   - `.sidebar-overlay` -- fixed full-screen dark overlay behind sidebar
   - `.hamburger` button -- three-line icon, hidden on desktop, visible below 768px
   - `.sidebar-link` -- vertical nav link styling for sidebar
   - `.page-header` -- inner page header section with padding-top for nav clearance
   - `.coming-soon` -- centered placeholder text style
   - Active nav link indicator (e.g., `color: var(--text-primary)` + bottom border accent)
   Keep the existing `.nav-links` class but rename to `.nav-links-desktop` to distinguish from mobile.

5. Create `src/scripts/animations.js` -- extract the IntersectionObserver logic and mouse glow effect from `main.js`. Use `document.addEventListener('astro:page-load', ...)` instead of `DOMContentLoaded` so it re-runs on View Transition navigations. Also include the navbar scroll effect here. Export nothing -- this is a side-effect script that will be imported in the layout.

6. Create `public/favicon.svg` -- a simple SyncTexts "S" lettermark SVG in the primary indigo color.

Do NOT delete the original `index.html`, `style.css`, or `main.js` yet -- they serve as reference. They can be removed after the full migration is verified.
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Astro project builds successfully. `src/styles/global.css` contains all design tokens and utilities from original `style.css` plus new mobile sidebar styles. `src/scripts/animations.js` uses `astro:page-load` event.</done>
</task>

<task type="auto">
  <name>Task 2: Create layout shell and navigation components</name>
  <files>
    src/layouts/BaseLayout.astro,
    src/components/Navigation.astro,
    src/components/Footer.astro,
    src/components/BackgroundGlows.astro,
    src/components/PageHeader.astro
  </files>
  <action>
1. Create `src/components/BackgroundGlows.astro` -- port the `.bg-glow` divs and `.mouse-glow` div from `index.html`. These are the three fixed-position gradient orbs plus the mouse-tracking glow. No props needed.

2. Create `src/components/Navigation.astro`:
   - Use `Astro.url.pathname` to detect current page for active link styling.
   - Define `navLinks` array: `[{href: '/', label: 'Home'}, {href: '/portfolio', label: 'Portfolio'}, {href: '/team', label: 'Team'}, {href: '/blog', label: 'Blog'}, {href: '/pricing', label: 'Pricing'}, {href: '/contact', label: 'Contact'}]`
   - Desktop nav: `.glass-nav` with `.nav-links-desktop` containing all 6 links plus "Start Project" CTA button linking to `/contact`.
   - Mobile nav: `.hamburger` button (three `<span>` lines) with `aria-expanded="false"` and `aria-label="Toggle navigation"`. A `.mobile-sidebar.glass-panel` div with `aria-hidden="true"` containing all nav links vertically plus CTA. A `.sidebar-overlay` div.
   - Active link: apply `active` class via `class:list` when `currentPath === link.href` (for `/`) or `currentPath.startsWith(link.href)` for other paths.
   - Include `<script>` tag using `document.addEventListener('astro:page-load', ...)` for hamburger toggle. Close sidebar on overlay click. Also close sidebar on `astro:before-preparation` event to prevent state persisting through View Transitions. Lock body scroll when sidebar open.

3. Create `src/components/Footer.astro`:
   - Port the existing footer structure from `index.html`.
   - Expand to a two-row layout: top row with logo + tagline on left and nav link columns on right (Quick Links, Services). Bottom row with copyright.
   - Use the glassmorphism border-top from existing footer.

4. Create `src/components/PageHeader.astro`:
   - Props: `title: string`, `subtitle?: string`
   - Renders a `<section class="page-header">` with `<h1 class="text-gradient">{title}</h1>` and optional subtitle paragraph.
   - Scoped styles: padding-top ~10rem (clears fixed nav), centered text, responsive font sizing via clamp().

5. Create `src/layouts/BaseLayout.astro`:
   - Props: `title: string`, `description?: string` (defaults to SyncTexts tagline).
   - Import `ClientRouter` from `astro:transitions`.
   - Import `../styles/global.css` in frontmatter.
   - `<head>`: charset, viewport, favicon, title (`{title} | SyncTexts`), description meta, Google Fonts preconnect + stylesheet link, `<ClientRouter />`.
   - `<body>`: `<BackgroundGlows />`, `<Navigation />`, `<main><slot /></main>`, `<Footer />`.
   - Import and include `src/scripts/animations.js` via a `<script>` tag.
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 components exist and build without errors. BaseLayout wraps content with nav, footer, background glows, and View Transitions. Navigation has desktop links, mobile sidebar, and hamburger toggle script.</done>
</task>

<task type="auto">
  <name>Task 3: Create all page routes with placeholder content</name>
  <files>
    src/pages/index.astro,
    src/pages/portfolio.astro,
    src/pages/team.astro,
    src/pages/blog.astro,
    src/pages/pricing.astro,
    src/pages/contact.astro
  </files>
  <action>
1. Create `src/pages/index.astro` as a minimal homepage shell for now (full content migration in Plan 02). Import BaseLayout. Wrap content in `<BaseLayout title="Premium Web &amp; DevOps Agency">`. For now, include the hero section HTML ported directly from `index.html` (badge, title, subtitle, CTA buttons) so the homepage is not empty. Update hero CTA links: "Partner With Us" -> `/contact`, "Explore Expertise" -> `/portfolio`. Other sections (services, tech, previews) come in Plan 02.

2. Create 5 inner pages, each following this pattern:
   ```
   import BaseLayout from '../layouts/BaseLayout.astro';
   import PageHeader from '../components/PageHeader.astro';
   ```
   Wrap in `<BaseLayout title="[Page Name]" description="[Brief description]">`.
   Include `<PageHeader title="[Page Name]" subtitle="[Brief subtitle]" />`.
   Add a `<section class="container section"><p class="coming-soon">Content coming soon.</p></section>`.

   Specific pages:
   - `portfolio.astro` -- title: "Portfolio", subtitle: "A selection of our finest work"
   - `team.astro` -- title: "Our Team", subtitle: "The engineers behind the code"
   - `blog.astro` -- title: "Blog", subtitle: "Insights, tutorials, and engineering stories"
   - `pricing.astro` -- title: "Pricing", subtitle: "Transparent pricing for every stage of growth"
   - `contact.astro` -- title: "Contact", subtitle: "Let's build something great together". Include the contact form HTML from `index.html` (the `.contact-wrapper` with form) below the PageHeader instead of the coming-soon text. Keep form submission as simulated (no backend until Phase 3).

3. Verify all 6 routes build to `dist/` correctly. Run `npm run build` and confirm `dist/index.html`, `dist/portfolio/index.html`, `dist/team/index.html`, `dist/blog/index.html`, `dist/pricing/index.html`, `dist/contact/index.html` all exist.
  </action>
  <verify>
    <automated>cd /Users/habib/Git/synctexts-website && npm run build && ls dist/index.html dist/portfolio/index.html dist/team/index.html dist/blog/index.html dist/pricing/index.html dist/contact/index.html</automated>
  </verify>
  <done>All 6 pages build successfully to dist/. Homepage shows hero section. Inner pages show PageHeader + placeholder content. Contact page includes the contact form. Navigation links resolve to correct routes.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds without errors
2. All 6 HTML files exist in `dist/` at correct paths
3. `grep -c "glass-nav" dist/index.html` returns at least 1 (nav is rendered)
4. `grep -c "hamburger" dist/index.html` returns at least 1 (mobile nav toggle exists)
5. `grep -c "ClientRouter" dist/index.html` returns 0 (View Transitions JS is injected, not the raw component name)
6. `grep -c "PageHeader" dist/portfolio/index.html` returns 0 (component is rendered to HTML, not raw)
</verification>

<success_criteria>
- Astro project builds with static output, no adapter
- 6 routes generate HTML: /, /portfolio, /team, /blog, /pricing, /contact
- Sticky glass nav renders on all pages with links to all 6 pages + CTA button
- Mobile sidebar markup present with hamburger toggle and glassmorphism styling
- Glassmorphism design tokens (glass-panel, text-gradient, buttons, reveals) available globally
- View Transitions enabled via ClientRouter
- Scripts use astro:page-load instead of DOMContentLoaded
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-migration/01-01-SUMMARY.md`
</output>
