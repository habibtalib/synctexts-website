---
phase: 09-cal-com-scheduling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/contact-form.ts
  - src/pages/contact.astro
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-03
  - CAL-05

must_haves:
  truths:
    - "Cal.com inline booking widget renders inside the success panel after form submission"
    - "Cal.com embed uses dark theme with indigo (#6366f1) brand color"
    - "Cal.com embed prefills name and email from the just-submitted form data"
    - "Cal.com embed script is loaded lazily only when success panel renders, not on page load"
    - "Navigating away and back to contact page re-initializes form without Cal.com embed errors"
  artifacts:
    - path: "src/scripts/contact-form.ts"
      provides: "injectCalEmbed function + Cal.com IIFE snippet injection"
      contains: "injectCalEmbed"
    - path: "src/pages/contact.astro"
      provides: "CSS for #cal-embed-container and #cal-embed-loading"
      contains: "#cal-embed-container"
  key_links:
    - from: "src/scripts/contact-form.ts"
      to: "Cal.com CDN"
      via: "IIFE snippet loading https://app.cal.com/embed/embed.js"
      pattern: "app.cal.com/embed/embed.js"
    - from: "src/scripts/contact-form.ts"
      to: "#cal-embed-container"
      via: "Cal('inline', { elementOrSelector: '#cal-embed-container' })"
      pattern: "cal-embed-container"
---

<objective>
Replace the "Book a Discovery Call" link in the contact form success panel with an inline Cal.com booking embed that prefills name and email, uses dark theme with indigo accent, and loads lazily after form submission.

Purpose: Enable visitors to book a discovery call directly from the contact page without leaving the site (CAL-01, CAL-02, CAL-03, CAL-05).
Output: Updated contact-form.ts with Cal.com embed injection + CSS for embed container.
</objective>

<execution_context>
@/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cal-com-scheduling/09-CONTEXT.md
@.planning/phases/09-cal-com-scheduling/09-RESEARCH.md
@.planning/phases/09-cal-com-scheduling/09-UI-SPEC.md

<interfaces>
<!-- Current success panel HTML in contact-form.ts (lines 528-541) -->
```typescript
const wrapper = document.querySelector<HTMLElement>('.contact-wrapper');
if (wrapper) {
  wrapper.innerHTML = `
    <div class="contact-info">
      <h2>Ready to <span class="text-gradient">Accelerate</span>?</h2>
      <p>Let's discuss how SyncTexts can elevate your next project. We are ready to tackle your toughest technical challenges.</p>
    </div>
    <div class="success-panel">
      <div class="success-icon">&#10003;</div>
      <h2>Message Sent!</h2>
      <p>We'll get back to you within 24 hours.</p>
      <a href="${CAL_URL}" target="_blank" rel="noopener noreferrer" class="btn btn-primary btn-lg">Book a Discovery Call</a>
    </div>
  `;
}
```

<!-- FormState interface (contact-form.ts line 11-20) -->
```typescript
interface FormState {
  step: 1 | 2 | 3;
  service_type: string | null;
  budget: string | null;
  timeline: string | null;
  name: string;
  email: string;
  company: string;
  message: string;
}
```

<!-- CAL_URL constant (contact-form.ts line 27) -->
```typescript
const CAL_URL = (import.meta as Record<string, unknown> & { env?: Record<string, string> }).env?.PUBLIC_CAL_URL ?? 'https://cal.com/synctexts/discovery';
```

<!-- Existing success panel CSS (contact.astro lines 187-216) -->
```css
.success-panel {
  text-align: center;
  padding: 3rem 2rem;
}
.success-icon { /* green checkmark circle */ }
.success-panel h2 { margin-bottom: 0.5rem; }
.success-panel p { color: var(--text-secondary, #94a3b8); }
.success-panel .btn { margin-top: 1.5rem; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Cal.com embed container CSS to contact.astro</name>
  <files>src/pages/contact.astro</files>
  <read_first>
    - src/pages/contact.astro (full file — see existing scoped styles, find .success-panel .btn rule)
    - .planning/phases/09-cal-com-scheduling/09-UI-SPEC.md (Component Inventory section — exact CSS for #cal-embed-container)
  </read_first>
  <action>
In the `<style>` block of `src/pages/contact.astro`, add the following CSS rules AFTER the existing `.success-panel .btn` rule (around line 216):

```css
#cal-embed-container {
  width: 100%;
  min-height: 500px;
  margin-top: 1.5rem;
  border-radius: 12px;
  overflow: hidden;
  position: relative;
}

#cal-embed-loading {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}

#cal-embed-loading::after {
  content: '';
  width: 24px;
  height: 24px;
  border: 2px solid rgba(255, 255, 255, 0.2);
  border-top-color: rgba(255, 255, 255, 0.6);
  border-radius: 50%;
  animation: cal-spin 0.6s linear infinite;
}

@keyframes cal-spin {
  to { transform: rotate(360deg); }
}

@media (max-width: 600px) {
  #cal-embed-container {
    min-height: 600px;
  }
}
```

Also wrap the `#cal-embed-loading::after` animation and the embed container opacity transition inside the existing `@media (prefers-reduced-motion: no-preference)` block pattern:

```css
@media (prefers-reduced-motion: reduce) {
  #cal-embed-loading::after {
    animation: none;
  }
}
```

Note: The scoped `<style>` in contact.astro uses `:global()` or plain selectors — since `#cal-embed-container` is injected via JS innerHTML, it won't be scoped by Astro. Use `:global(#cal-embed-container)` wrapper if the style block is scoped, OR verify that contact.astro uses `is:global` or unscoped styles. Check the file first.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/contact.astro contains `#cal-embed-container`
    - src/pages/contact.astro contains `min-height: 500px`
    - src/pages/contact.astro contains `#cal-embed-loading`
    - src/pages/contact.astro contains `cal-spin`
    - src/pages/contact.astro contains `prefers-reduced-motion` near the cal-spin animation or loading section
    - src/pages/contact.astro contains `min-height: 600px` inside a `max-width: 600px` media query
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>CSS for Cal.com embed container, loading spinner, responsive sizing, and reduced-motion support added to contact.astro scoped styles</done>
</task>

<task type="auto">
  <name>Task 2: Replace CTA link with Cal.com inline embed in contact-form.ts</name>
  <files>src/scripts/contact-form.ts</files>
  <read_first>
    - src/scripts/contact-form.ts (full file — understand state variable scope, success panel innerHTML, CAL_URL constant, astro:page-load pattern)
    - .planning/phases/09-cal-com-scheduling/09-RESEARCH.md (Pattern 1: Cal.com Embed Script Injection, Pattern 4: Success Panel HTML, Code Examples section)
    - .planning/phases/09-cal-com-scheduling/09-UI-SPEC.md (Interaction States — loading, loaded, load error)
  </read_first>
  <action>
Make these changes to `src/scripts/contact-form.ts`:

**1. Add Cal.com global type declaration** at the top, inside the existing `declare global` block (lines 5-8):

```typescript
declare global {
  interface Window {
    dataLayer: Record<string, unknown>[];
    Cal?: ((...args: unknown[]) => void) & { loaded?: boolean; ns?: Record<string, unknown>; q?: unknown[] };
  }
}
```

**2. Add `injectCalEmbed` function** BEFORE the `document.addEventListener('astro:page-load', ...)` call. Place it after the constants section (after line 27). The function:

```typescript
function injectCalEmbed(name: string, email: string): void {
  const EMBED_SCRIPT_ID = 'cal-embed-script';
  const CAL_LINK = 'synctexts/discovery';

  // Inject Cal.com IIFE snippet once — guard against double-injection
  if (!document.getElementById(EMBED_SCRIPT_ID)) {
    const snippet = document.createElement('script');
    snippet.id = EMBED_SCRIPT_ID;
    snippet.textContent = `(function(C,A,L){var p=function(a,ar){a.q.push(ar)};var d=C.document;C.Cal=C.Cal||function(){var cal=C.Cal;var ar=arguments;if(!cal.loaded){cal.ns={};cal.q=cal.q||[];d.head.appendChild(d.createElement("script")).src=A;cal.loaded=true;}if(ar[0]===L){var api=function(){p(api,arguments)};var namespace=ar[1];api.q=api.q||[];if(typeof namespace==="string"){cal.ns[namespace]=cal.ns[namespace]||api;p(cal.ns[namespace],ar);p(cal,["initNamespace",namespace]);}else p(cal,ar);return;}p(cal,ar);};})(window,"https://app.cal.com/embed/embed.js","init");`;
    document.head.appendChild(snippet);
  }

  // Guard: only proceed if embed container exists in DOM
  const container = document.getElementById('cal-embed-container');
  if (!container) return;

  // Initialize Cal.com inline embed with prefill and dark theme
  window.Cal!('init', { origin: 'https://cal.com' });
  window.Cal!('inline', {
    elementOrSelector: '#cal-embed-container',
    calLink: CAL_LINK,
    config: {
      name,
      email,
      theme: 'dark',
    },
  });

  // Apply indigo brand color matching site's --primary
  window.Cal!('ui', {
    styles: {
      branding: { brandColor: '#6366f1' },
    },
    hideEventTypeDetails: false,
  });

  // Remove loading spinner once embed renders
  // Cal.com fires postMessage when ready — listen for it
  const removeSpinner = (): void => {
    const spinner = document.getElementById('cal-embed-loading');
    if (spinner) spinner.remove();
  };

  // Fallback: remove spinner after 5 seconds regardless
  setTimeout(removeSpinner, 5000);

  // Listen for Cal.com embed loaded message
  const handleMessage = (event: MessageEvent): void => {
    if (typeof event.data === 'string') {
      try {
        const parsed = JSON.parse(event.data);
        if (parsed.originator === 'CAL' || parsed.type?.startsWith?.('CAL:')) {
          removeSpinner();
          window.removeEventListener('message', handleMessage);
        }
      } catch {
        // Not a Cal.com message — ignore
      }
    }
  };
  window.addEventListener('message', handleMessage);

  // GTM tracking for Cal.com embed shown
  window.dataLayer = window.dataLayer || [];
  window.dataLayer.push({ event: 'cal_embed_shown' });

  // Listen for booking confirmation via bookingSuccessfulV2
  window.Cal!('on', {
    action: 'bookingSuccessfulV2',
    callback: (e: { detail: { data: { uid: string; title: string; startTime: string } } }) => {
      window.dataLayer = window.dataLayer || [];
      window.dataLayer.push({
        event: 'cal_booking_created',
        booking_uid: e.detail.data.uid,
      });
    },
  });
}
```

**3. Replace the CTA link in success panel innerHTML** (around line 538). Change:

```typescript
<a href="${CAL_URL}" target="_blank" rel="noopener noreferrer" class="btn btn-primary btn-lg">Book a Discovery Call</a>
```

To:

```typescript
<div id="cal-embed-container">
  <div id="cal-embed-loading" role="status" aria-label="Loading booking calendar"></div>
</div>
```

**4. Call `injectCalEmbed` after setting innerHTML.** Immediately after the `wrapper.innerHTML = ...` closing backtick+semicolon (line ~540), add:

```typescript
injectCalEmbed(state.name.trim(), state.email.trim());
```

The `state` variable is in scope here because the success handler is inside the form event listener where `state` is accessible.

**5. Add error fallback.** After the `injectCalEmbed` call, add a fallback timer:

```typescript
// Fallback: if Cal.com embed fails to load after 10s, show link button instead
setTimeout(() => {
  const container = document.getElementById('cal-embed-container');
  if (container && !container.querySelector('iframe')) {
    container.innerHTML = `<a href="${CAL_URL}" target="_blank" rel="noopener noreferrer" class="btn btn-primary btn-lg" style="display:inline-block;margin-top:1.5rem;">Book a Discovery Call</a>`;
  }
}, 10000);
```

**6. Keep the `CAL_URL` constant** on line 27 as-is — it serves as the fallback link URL.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/scripts/contact-form.ts contains `function injectCalEmbed(name: string, email: string)`
    - src/scripts/contact-form.ts contains `cal-embed-script` (double-injection guard ID)
    - src/scripts/contact-form.ts contains `app.cal.com/embed/embed.js` (CDN URL in IIFE snippet)
    - src/scripts/contact-form.ts contains `elementOrSelector: '#cal-embed-container'`
    - src/scripts/contact-form.ts contains `calLink: CAL_LINK` or `calLink: 'synctexts/discovery'`
    - src/scripts/contact-form.ts contains `theme: 'dark'` inside the Cal inline config
    - src/scripts/contact-form.ts contains `brandColor: '#6366f1'`
    - src/scripts/contact-form.ts contains `bookingSuccessfulV2` (GTM tracking callback)
    - src/scripts/contact-form.ts contains `id="cal-embed-container"` in the success panel innerHTML
    - src/scripts/contact-form.ts contains `injectCalEmbed(state.name` (call with form data)
    - src/scripts/contact-form.ts does NOT contain `<a href="${CAL_URL}"` inside the success panel innerHTML (the old CTA link is removed)
    - src/scripts/contact-form.ts DOES still contain `const CAL_URL` (kept as fallback)
    - src/scripts/contact-form.ts contains `role="status"` and `aria-label` on the loading element
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Success panel shows inline Cal.com booking embed with dark theme, indigo accent, name/email prefill, loading spinner, error fallback link, and GTM tracking for embed shown and booking created events</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- contact-form.ts has injectCalEmbed function with Cal.com IIFE snippet
- Success panel HTML contains #cal-embed-container instead of CTA link
- Cal.com embed configured with theme: 'dark', brandColor: '#6366f1', prefill name+email
- contact.astro has CSS for embed container sizing and loading spinner
</verification>

<success_criteria>
- Cal.com inline embed replaces CTA link in success panel
- Embed loads lazily only after form submission (no Cal.com JS on initial page load)
- Dark theme with indigo brand color applied
- Name and email prefilled from form state
- Loading spinner shown while embed loads with reduced-motion support
- Fallback link shown if embed fails to load after 10 seconds
- Double-injection guard prevents re-adding script on View Transitions
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-cal-com-scheduling/09-02-SUMMARY.md`
</output>
